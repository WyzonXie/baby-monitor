# 婴儿监护系统 · 软件设计文档（SDD）

  | 项目 | 内容 |
  |---|---|
  | 项目名 | baby-monitor（婴儿监护系统） |
  | 文档版本 | **v2.1** |
  | 日期 | 2026-07-30 |
  | 状态 | 阶段 1 已实现并通过测试；阶段 2+ 设计完成，待开发 |
  | 本文替代 | `DESIGN.md` v1.0、`constitution-draft.md`
  v1.0.0、`spec-draft-001.md`、`spec-draft-002.md`、`plan-draft-002.md`、`tasks-draft-0
  02.md`（六份已合并至本文并删除） |

  > **这份文档是本项目唯一的设计依据。**
  > 后面每一个功能、每一行代码、每一次 AI
  生成的结果，都要拿本文对照检查。与本文冲突的实现，无论多好用，都要打回。
  >
  > **阅读建议**：§1–§5 是"要做什么"，非技术背景也能读完；§6–§10 是"怎么做"；§14
  是所有关键设计决策的取舍记录，**面试时要讲的就是这一章**。

  ---

  ## 目录

  | # | 章节 | 内容 |
  |---|---|---|
  | 1 | [项目背景与目标](#1-项目背景与目标) | 要解决什么问题，明确不做什么 |
  | 2 | [项目宪法](#2-项目宪法十条不可谈判条款) | 十条不可谈判条款 |
  | 3 | [干系人与使用场景](#3-干系人与使用场景) | 谁在用，什么时候用 |
  | 4 | [需求](#4-需求) | 功能需求 + 非功能需求 |
  | 5 | [告警规则表](#5-告警规则表核心业务逻辑) | 核心业务逻辑 |
  | 6 | [系统架构](#6-系统架构) | 两层级联 |
  | 7 | [技术选型与理由](#7-技术选型与理由) | 选了什么，为什么，否决了什么 |
  | 8 | [详细设计](#8-详细设计) | 状态机、参数、感知算法 |
  | 9 | [数据与隐私](#9-数据与隐私) | 什么数据会离开家 |
  | 10 | [部署与运行环境](#10-部署与运行环境) | 硬件、安装、分发 |
  | 11 | [测试策略](#11-测试策略) | 怎么证明它是对的 |
  | 12 | [风险与已知限制](#12-风险与已知限制) | 做不到的事，如实说 |
  | 13 | [路线图与任务拆解](#13-路线图与任务拆解) | 分几步做 |
  | 14 | [设计决策记录（ADR）](#14-设计决策记录adr) | **每个决策的取舍与理由** |
  | 15 | [附录](#15-附录) | 术语表、开源清单、操作步骤 |

  ---

  ## 1. 项目背景与目标

  ### 1.1 问题陈述

  家长白天上班不在家，婴儿（约 6 个月）由保姆或长辈照看。家长无法确认：

  - 宝宝是不是在哭、哭了多久
  - 哭的时候**有没有人在管**
  - 有没有翻身趴着、脸被遮住
  - 床上有没有出现不该有的小东西
  - **系统本身还活着吗**（这条最容易被忽略，见 §8.4）

  现有的商用摄像头 App 只提供"看直播"，需要人**主动去看**。
  **本项目要解决的是：不用盯着屏幕，有事它主动找你。**

  ### 1.2 目标

  | 编号 | 目标 |
  |---|---|
  | G1 | 婴儿持续哭泣**且无人照看**时，主动推送到家长手机 |
  | G2 | 检测危险体位（俯卧脸不可见、脸被遮挡） |
  | G3 | 检测婴儿床内出现的陌生小物件（误食的**前置**防御） |
  | G4 | 全天候自证存活（心跳），系统挂了家长必须知道 |
  | G5 | 误报率控制在**每晚 < 1 次** |
  | G6 | 长期运行成本 ≤ **5 元 / 月** |

  ### 1.3 非目标（明确不做，同样重要）

  | 编号 | 不做 | 原因 |
  |---|---|---|
  | NG1 | **不是医疗设备，不预防 SIDS（婴儿猝死综合征）** |
  无临床验证；宣称医疗用途误导用户且涉及监管风险 |
  | NG2 | **不做呼吸 / 心率监测** | 视频测呼吸精度不足以支撑告警。**"没动作 =
  没呼吸"是错误推论**——睡着的婴儿本来就几乎不动 |
  | NG3 | **不做"接住正在窒息的宝宝"** | 物理上做不到，见 §12.2。假防御比没防御更危险 |
  | NG4 | 不做哭声**原因**分类（饿了 / 困了 / 尿了） | 学术上不成立，见 §7.5 |
  | NG5 | 不做 24 小时连续录像 | 隐私红线，见 §9 |
  | NG6 | **不做实时直播 / 网页端 / 手机 App** | 摄像头自带 App
  已经能看直播，不重复造轮子。见 ADR-06 |
  | NG7 | 不做商业化（自用 + 送朋友） | 因此非商用许可证的模型可用，见 §15.2 |

  ---

  ## 2. 项目宪法（十条不可谈判条款）

  > 这是项目的"上位法"。下面所有章节的设计都必须能通过这十条的检查。

  ### 第一条：这不是医疗设备

  系统是**辅助观察工具**，不是安全设备、不是医疗设备。

  - 不得声称、不得暗示能预防 SIDS
  - 不得实现呼吸监测、心率监测等健康体征功能
  - 界面与文档中必须保留免责说明

  **为什么**：无临床验证。宣称医疗能力既误导用户，也带来监管风险。

  ### 第二条：宁可漏报，不可误报

  当"多报一次"与"少报一次"冲突时，**一律选择少报**。误报率目标 **< 1 次 /
  晚**，达不到的规则宁可不上线。

  **为什么**：杀死这类系统的从来不是漏报，而是误报。15 次告警里 14
  次是假的，用户第二天就会静音它——**一个被静音的系统，保护力为零。**

  ### 第三条：所有告警必须有时间阈值

  **禁止瞬时触发**。任何规则都必须包含"持续 N 秒 / 分钟"的条件。

  **为什么**：瞬时触发等于误报制造机。宝宝动一下、光线闪一下都会触发。

  ### 第四条：系统必须自证存活

  系统必须周期性主动报告"我还活着"。

  **为什么**：用户不在家时，"程序崩溃了"和"一切正常"在手机上长得**完全一样**——都是没消
  息。心跳把"没消息"从两义变成一义：**收不到心跳 = 出事了。**

  心跳不是可选功能，是第二条之外的第二道生命线。

  ### 第五条：不做假防御

  如果一个功能在物理上无法达成其宣称的效果，**明确不做，并写清楚为什么不做**。

  **为什么**：假的防御比没有防御更危险——用户会因此放松警惕。

  **已适用案例**：「接住正在窒息的宝宝」被否决，降级为**预防性巡检**（异物出现即告警，
  此时还没进嘴）。见 §12.2。

  ### 第六条：隐私最小化

  - **最小采集**：镜头只对准婴儿床，不覆盖房间其他区域
  - **最小留存**：只保存**触发时刻**的画面，禁止 24 小时连续录像
  - **不出户**：视频默认不离开家庭网络；对外只传触发时刻的少量帧
  - **知情**：必须告知看护人摄像头的存在与用途

  **为什么**：被拍的人（婴儿、看护人）没有同意能力或没有议价能力，采集方必须自我约束。

  ### 第七条：密钥零提交

  任何密钥、Token、Webhook 地址**不得进入版本库**。代码中只保留占位符，提供
  `config.example.py`，`.gitignore` 必须覆盖真实配置文件。

  **为什么**：Git
  有完整历史，**提交过之后再删掉是无效的**，仍然能被翻出来。这是新手最常见的事故。

  ### 第八条：阈值必须有数据依据

  任何数值阈值都必须在文档中写明取值理由，有日志数据支撑，或明确标注"待实地调参"。**禁
  止拍脑袋定值后不做验证。**

  **为什么**：说不出"为什么是 0.25 而不是 0.3"，这个系统就不是工程，是玄学。

  ### 第九条：规则逻辑必须有自动化测试

  每条告警规则合并前必须有自动化测试，至少覆盖：该触发时触发、**不该触发时不触发**（这
  条更重要，见第二条）、边界条件（时长刚好卡在阈值上）。

  ### 第十条：成本有上限

  长期运行成本 ≤ **5 元 /
  月**。任何方案在采纳前必须估算长期成本，超限的必须改造或否决。

  ### 宪法修订记录

  | 版本 | 日期 | 变更 |
  |---|---|---|
  | 1.0.0 | 2026-07-23 | 初版 |
  | 1.0.1 | 2026-07-23 | 并入本文档 §2，内容未变 |

  ---

  ## 3. 干系人与使用场景

  ### 3.1 干系人

  | 角色 | 关注点 |
  |---|---|
  | **家长（主用户）** | 不在家时的安心感；不想被误报骚扰 |
  | **看护人（保姆 / 长辈）** | **知情权**——必须被告知有摄像头及其用途 |
  | **婴儿（约 6 个月）** | 隐私；不被 850nm 红外灯的可见红光影响睡眠 |

  ### 3.2 主场景：白天（**本系统的主要场景**）

  ```
  09:00  家长出门上班，看护人在家
  09:00  监护程序在家里的 Windows PC 上持续运行
  10:00  收到心跳："✅ 系统正常，一切安静"
  11:32  宝宝开始哭 → 持续 3 分钟，画面里始终没有大人
         → 🔴 微信推送 + 当时的抓拍图
  11:33  家长打电话给看护人；想看现场就打开摄像头自带的 App
  12:00  收到心跳
  ...
  18:30  家长回家，关掉程序
  ```

  ### 3.3 次要场景：夜间（**附赠，不做承诺**）
  程序可以整夜运行、规则照常生效，但**本系统不承诺能在夜里叫醒你**——微信消息穿不透手机
  静音与勿扰模式（见
  §12.3）。夜间价值主要在于**第二天早上能回看日志**，以及万一你醒着就能收到。

  ### 3.4 关键约束

  - **家长不在现场**，因此"系统静默"和"一切正常"看起来完全一样 → **心跳是刚需**
  - 婴儿约 6 个月，主要在婴儿床内，摄像头视野固定、距离可控
  - 双端（iOS + Android）都要能收到
  - **宝宝房间常年开着白噪音机 / 加湿器 / 空气净化器** →
  持续底噪会淹没哭声，**这是本项目最主要的漏报风险来源**（见 §8.3、§11.3）
  - 家里**没有宠物、没有更大的孩子** → 哭声误报源少，灵敏度可适当调低以补偿底噪

  ---

  ## 4. 需求

  ### 4.1 功能需求

  | 编号 | 需求 | 阶段 |
  |---|---|---|
  | FR-01 | 持续采集麦克风音频，识别婴儿哭声 | 1 ✅ |
  | FR-02 | 哭声**持续**超过阈值时推送告警 | 1 ✅ |
  | FR-03 | 短暂的哭声中断（换气）**不得**重置持续计时 | 1 ✅ |
  | FR-04 | 告警后有静默期，避免刷屏 | 1 ✅ |
  | FR-05 | 定时心跳，自证存活 | 1 ✅ |
  | FR-06 | 全量打分写入 CSV 日志，供后续调参与复盘 | 1 ✅ |
  | FR-07 | 通知渠道未配置或故障时**不崩溃**，继续工作 | 1 ✅ |
  | FR-08 | 所有可调参数集中在一处，非技术用户可修改 | 1 ✅ |
  | FR-09 | 采集摄像头画面（RTSP） | 2 |
  | FR-10 | 检测画面中的成人（人数统计） | 2 |
  | FR-11 | 联合规则："哭 + 全程无成人" → 升级告警 | 2 |
  | FR-12 | 断流自动重连；画面异常（遮挡/全黑/卡死）识别 | 2 |
  | FR-13 | 婴儿姿态识别（俯卧 / 脸遮挡 / 出框） | 3 |
  | FR-14 | 触发时抓拍，随告警发送图片 | 3 |
  | FR-15 | 越界检测（正在翻越床栏 / 手脚卡栏杆） | 3 |
  | FR-16 | 一次性配置工具：在画面上框出床区域与栏杆顶沿 | 3 |
  | FR-17 | 大模型 API 二次确认，抑制误报 | 4 |
  | FR-18 | 婴儿床内陌生物件检测（白名单外） | 6 |
  | FR-19 | 空床基线学习（用于 FR-18） | 6 |

  ### 4.2 非功能需求

  | 编号 | 需求 | 指标 |
  |---|---|---|
  | NFR-01 | 误报率 | **< 1 次 / 晚**（第一优先） |
  | NFR-02 | 告警端到端延迟 | < 15 秒 |
  | NFR-03 | CPU 占用 | 音频层单核 < 5% |
  | NFR-04 | 运行成本 | ≤ 5 元 / 月 |
  | NFR-05 | **网络要求** | **仅需出站 HTTPS**。不需要公网
  IP、不需要端口映射、不需要域名、不需要备案 |
  | NFR-06 | 崩溃可见性 | 程序死亡后 ≤ 1 小时内家长察觉 |
  | NFR-07 | 隐私 | 视频默认不出家门；仅保存触发时刻画面 |
  | NFR-08 | 易用性 | 非技术用户从安装到跑起来 ≤ 30 分钟，**不需要敲命令行** |
  | NFR-09 | 稳定性 | 连续运行 ≥ 12 小时不中断 |

  > **NFR-05 是一条硬约束，它排除了一整类设计。** 任何需要"从外网访问家里"的方案（网页
  端、直播服务、内网穿透）都违反它，因此全部不做（NG6 / ADR-06）。

  ---

  ## 5. 告警规则表（核心业务逻辑）

  这是整个系统的核心。规则的取舍针对"约 6 个月 + 白天家长不在家"这个具体场景。

  | # | 级别 | 触发条件 | 说明 | 阶段 |
  |---|---|---|---|---|
  | **R1** | 🔴 | **俯卧 且 脸不可见 > 30 秒** | 最高优先级：刚学会翻身但翻不回去 | 3 |
  | **R2** | 🔴 | **脸被遮挡（被子/毛巾）> 20 秒** | | 3 |
  | **R3** | 🔴 | **哭 > 3 分钟 且 全程画面内无成人** | 音频 + 视觉联合判断 |
  1（音频）/ 2（联合） |
  | **R4** | 🔴 | **婴儿离开画面 > 60 秒** | 覆盖：掉下床 / 被抱走 / 镜头被挡 | 3 |
  | **R5** | 🔴 | **身体越过床栏顶沿 > 3 秒** | 正在往外翻。6
  个月用不上，会扶站后是救命规则 | 3 |
  | **R6** | 🟡 | 婴儿床内出现白名单外的物体 > 30 秒 | 误食**前置**防御，见 §12.2 | 6 |
  | **R7** | 🟡 | 翻身成俯卧（脸仍可见）> 30 秒 | 预警，未达红色 | 3 |
  | **R8** | 🟡 | 手或脚伸出床栏外 且 20 秒内几乎不动 |
  疑似卡住。**误报风险最高的一条**，见 §12.4 | 3 |
  | **R9** | 🟡 | 画面内 > 2 小时无成人 | 看护人长时间离岗 | 2 |
  | **R10** | 🟢 | 每 2 小时状态摘要 | | 2 |
  | **R11** | ⚙️ | **每 1 小时心跳** | 系统存活证明（宪法第四条） | 1 ✅ |

  **推送形式**：🔴 级发送**文字 + 抓拍图**；🟡 🟢 ⚙️ 仅文字。

  ### 5.1 设计要点

  - **R3 是联合规则，单纯"哭"不告警。** 宝宝哭而有人在管，是正常育儿场景，报了就是误报
  。这是本系统与市面上"哭了就响"的产品最大的区别。
  - **所有规则都有时间阈值**（宪法第三条），没有任何瞬时触发。
  - **R6
  是预防性的，不是急救性的**（§12.2）。它在东西还没进嘴时就报，而不是等到宝宝往嘴里塞。
  - **R5 / R8 是新增的**，原设计假设"6
  **为什么**：说不出"为什么是 0.25 而不是 0.3"，这个系统就不是工程，是玄学。

  ### 第九条：规则逻辑必须有自动化测试

  每条告警规则合并前必须有自动化测试，至少覆盖：该触发时触发、**不该触发时不触发**（这
  条更重要，见第二条）、边界条件（时长刚好卡在阈值上）。

  ### 第十条：成本有上限

  长期运行成本 ≤ **5 元 /
  月**。任何方案在采纳前必须估算长期成本，超限的必须改造或否决。

  ### 宪法修订记录

  | 版本 | 日期 | 变更 |
  |---|---|---|
  | 1.0.0 | 2026-07-23 | 初版 |
  | 1.0.1 | 2026-07-23 | 并入本文档 §2，内容未变 |

  ---

  ## 3. 干系人与使用场景

  ### 3.1 干系人

  | 角色 | 关注点 |
  |---|---|
  | **家长（主用户）** | 不在家时的安心感；不想被误报骚扰 |
  | **看护人（保姆 / 长辈）** | **知情权**——必须被告知有摄像头及其用途 |
  | **婴儿（约 6 个月）** | 隐私；不被 850nm 红外灯的可见红光影响睡眠 |

  ### 3.2 主场景：白天（**本系统的主要场景**）

  ```
  09:00  家长出门上班，看护人在家
  09:00  监护程序在家里的 Windows PC 上持续运行
  10:00  收到心跳："✅ 系统正常，一切安静"
  11:32  宝宝开始哭 → 持续 3 分钟，画面里始终没有大人
         → 🔴 微信推送 + 当时的抓拍图
  11:33  家长打电话给看护人；想看现场就打开摄像头自带的 App
  12:00  收到心跳
  ...
  18:30  家长回家，关掉程序
  ```

  ### 3.3 次要场景：夜间（**附赠，不做承诺**）
  | 只用本地小模型 | 低 | 免费，但**误报多到没法用** | ❌ 违反宪法第二条 |
  | **两层级联** | **高** | **每月几元** | ✅ |

  本地层负责**把 99% 的平静时间挡掉**，大模型只在剩下 1% 的可疑时刻被唤醒。

  > **这是成本工程，不是技术炫技。** 宪法第二条（误报是生死线）和第十条（成本 ≤ 5
  元/月）单独看都做不到，两层级联是同时满足它们的唯一结构。

  ### 6.3 组件划分

  | 组件 | 职责 | 状态 |
  |---|---|---|
  | `audio_watcher` | 麦克风采集 + 哭声打分 | ✅ 阶段 1 |
  | `cry_state_machine` | 迟滞状态机，输出"在哭 / 不哭" | ✅ 阶段 1 |
  | `notifier` | 企业微信推送（文字 / 图片） | ✅ 阶段 1（图片待补） |
  | `heartbeat` | 定时存活消息 | ✅ 阶段 1 |
  | `logger` | 全量打分落 CSV | ✅ 阶段 1 |
  | `video_watcher` | RTSP 采集、断流重连、画面异常检测 | ⬜ 阶段 2 |
  | `person_detector` | 成人检测 / 计数、成人与婴儿区分 | ⬜ 阶段 2 |
  | `pose_estimator` | 婴儿姿态、头部朝向 | ⬜ 阶段 3 |
  | `zone_config` | 一次性配置工具：标定床区域与栏杆顶沿 | ⬜ 阶段 3 |
  | `rule_engine` | 多模态规则融合、抑制、冷却 | ⬜ 阶段 3 |
  | `llm_verifier` | 大模型二次确认 | ⬜ 阶段 4 |
  | `object_scanner` | 空床基线 + 床内异物扫描 | ⬜ 阶段 6 |

  ### 6.4 数据流（阶段 1，已实现）

  ```
  麦克风
    │ 16kHz 单声道
    ▼
  1 秒切片 ──► YAMNet ──► 521 类得分
                            │ 取 Baby cry / Crying,sobbing 的最大值
                            ▼
                      哭声分数 (0.0 ~ 1.0)
                            │ ≥ CRY_THRESHOLD(0.25) → 记 1，否则记 0
                            ▼
                    最近 30 秒滑动窗口 → 计算 1 的比例
                            ▼
                      迟滞状态机（§8.1）
                      进入: 比例 ≥ 45% → "在哭"
                      退出: 比例 ≤ 20% → "不哭"
                            ▼
                连续在哭 ≥ 180 秒 且 不在冷却期
                            ▼
                      企业微信推送 → 冷却 600 秒
  ```

  ---

  ## 7. 技术选型与理由

  ### 7.1 音频识别：YAMNet

  | 项 | 内容 |
  |---|---|
  | 选型 | **YAMNet**（Google，AudioSet 训练，521 类） |
  | 运行框架 | MediaPipe Tasks Audio（`AudioClassifier`） |
  | 许可证 | **Apache-2.0** ✅ |
  | 模型大小 | 4.1 MB |
  | 用到的类 | `Baby cry, infant cry`(167)、`Crying,
  sobbing`(327)、`Whimper`(359)、`Baby laughter`(515)、`Speech`(13) |
  | 实测性能 | 1 秒音频分类耗时 **5 ms**（约 200× 实时），CPU 占用可忽略 |

  **为什么选它**：Apache 许可干净、体积小、CPU 就能跑、类别里**直接有"婴儿哭"**、Google
  长期维护。

  ### 7.2 视觉：分层选型

  | 用途 | 选型 | 许可证 |
  |---|---|---|
  | 成人检测 / 计数 | RT-DETR / RF-DETR | Apache-2.0 ✅ |
  | 通用姿态 | MediaPipe Pose | Apache-2.0 ✅ |
  | **婴儿专用姿态** | ACLab 系列（Infant-Pose-Estimation、CribNet、SyRIP、AggPose） |
  ⚠️ 非商用 |

  > **许可证关键判断**：几乎**所有**婴儿专用视觉模型都是非商用或
  AGPL。因为本项目明确为自用 + 送朋友（NG7），这些限制不适用，可以放心用。
  > **一旦要商业化，整条视觉链必须重做。** 这一点必须写在 README 里。

  ### 7.3 推送通道：企业微信群机器人

  | 项 | 内容 |
  |---|---|
  | 接口 | `POST https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=KEY` |
  | 文字 | `{"msgtype":"text","text":{"content":"..."}}` |
  | 图片 | `{"msgtype":"image","image":{"base64":"...","md5":"..."}}`，**上限 2 MB** |
  | **限流** | **20 条 / 分钟**（需节流，见 §8.6） |
  | ⚠️ 约束 | **`image` 类型不支持 @所有人** → 🔴
  级告警必须拆成两条连发（先文字后图片） |
  | ⚠️ 约束 | `news` 图文卡片的封面图需**公网可访问 URL** → 与 NFR-05
  冲突，**不可用，已放弃** |
  | 费用 | 个人可用，无需营业执照，**免费** |
  | 双端 | iOS + Android 均支持 ✅ |
  **为什么**：说不出"为什么是 0.25 而不是 0.3"，这个系统就不是工程，是玄学。

  ### 第九条：规则逻辑必须有自动化测试

  每条告警规则合并前必须有自动化测试，至少覆盖：该触发时触发、**不该触发时不触发**（这
  条更重要，见第二条）、边界条件（时长刚好卡在阈值上）。

  ### 第十条：成本有上限

  长期运行成本 ≤ **5 元 /
  月**。任何方案在采纳前必须估算长期成本，超限的必须改造或否决。

  ### 宪法修订记录

  | 版本 | 日期 | 变更 |
  |---|---|---|
  | 1.0.0 | 2026-07-23 | 初版 |
  | 1.0.1 | 2026-07-23 | 并入本文档 §2，内容未变 |

  ---

  ## 3. 干系人与使用场景

  ### 3.1 干系人

  | 角色 | 关注点 |
  |---|---|
  | **家长（主用户）** | 不在家时的安心感；不想被误报骚扰 |
  | **看护人（保姆 / 长辈）** | **知情权**——必须被告知有摄像头及其用途 |
  | **婴儿（约 6 个月）** | 隐私；不被 850nm 红外灯的可见红光影响睡眠 |

  ### 3.2 主场景：白天（**本系统的主要场景**）

  ```
  09:00  家长出门上班，看护人在家
  09:00  监护程序在家里的 Windows PC 上持续运行
  10:00  收到心跳："✅ 系统正常，一切安静"
  11:32  宝宝开始哭 → 持续 3 分钟，画面里始终没有大人
         → 🔴 微信推送 + 当时的抓拍图
  11:33  家长打电话给看护人；想看现场就打开摄像头自带的 App
  12:00  收到心跳
  ...
  18:30  家长回家，关掉程序
  ```

  ### 3.3 次要场景：夜间（**附赠，不做承诺**）
  ## 8. 详细设计

  ### 8.1 迟滞状态机（Hysteresis）—— 阶段 1 核心

  **问题**：宝宝哭的时候会停下来换气。如果用单一阈值判断，状态会在"哭 /
  不哭"之间疯狂反复横跳，计时器不断被重置，**永远攒不够 3 分钟**。

  **方案**：**进入和退出用两个不同的阈值**。

  ```
  比例 ≥ 45%  ──► 进入"在哭"
  比例 ≤ 20%  ──► 退出"在哭"
  20% ~ 45%   ──► 保持当前状态不变   ← 这段就是"迟滞带"
  ```

  换气造成的短暂下降（比例掉到 30%）落在迟滞带内，不会触发退出。

  > **类比**：空调设定 26℃，不会到 26.0℃ 就停、25.9℃ 就开，而是 27℃ 开、25℃ 停。中间那
  2 度就是迟滞带——防止压缩机反复启停烧坏。

  ### 8.2 关键参数

  | 参数 | 值 | 理由 |
  |---|---|---|
  | `SAMPLE_RATE` | 16000 Hz | YAMNet 硬性要求 |
  | `CHUNK_SECONDS` | 1.0 | 分类粒度 |
  | `SMOOTH_WINDOW` | 30 秒 | 平滑窗口，覆盖一个完整的哭—换气周期 |
  | `ENTER_RATIO` | 0.45 | 进入阈值 |
  | `EXIT_RATIO` | 0.20 | 退出阈值，与进入值拉开形成迟滞带 |
  | `CRY_THRESHOLD` | **0.25** | 单帧判定为哭的分数线，**⚠️ 唯一必须实地调的参数** |
  | `CRY_ALERT_SECONDS` | 180 | 哭满 3 分钟才报警 |
  | `ALERT_COOLDOWN_SECONDS` | 600 | 报警后 10 分钟静默 |
  | `HEARTBEAT_SECONDS` | 3600 | 每小时心跳 |

  > **宪法第八条要求**：以上除 `SAMPLE_RATE`
  外全部为**暂定值**，必须经一周实地数据验证后定稿。程序把每一秒的打分写进
  CSV，就是为了让你用真实数据回头调它们。

  ### 8.3 ⚠️ 白噪音机对阈值的影响（本项目最主要的漏报风险）

  宝宝房间常年开着白噪音机 / 加湿器 / 空气净化器。这带来一个**方向与直觉相反**的问题：

  - **白噪音本身不会被误判为哭。**（阶段 1 测试已验证：白噪声打分 0.0000）
  - **但它会淹没哭声。** 持续底噪拉低信噪比，YAMNet
  对哭声的打分整体下移，可能长期够不着 0.25 这条线 → **漏报**。

  **应对**：
  1. 实地录制"白噪音机开着 + 宝宝真实哭声"的音频，看实际打分分布
  2. 根据分布**下调** `CRY_THRESHOLD`
  3.
  家里**没有宠物、没有更大的孩子**（§3.4），哭声误报源本来就少，因此下调阈值的代价可控
  4. 若下调后仍不可靠，备选：在音频进入 YAMNet
  前做一次谱减法降噪（用一段纯白噪音作为噪声基线）

  > 这条必须实测，不能推算。写进阶段 5 的实地调参任务。

  ### 8.4 心跳设计（宪法第四条）

  **为什么是刚需**：家长不在家时，"程序崩溃了"和"一切正常没事发生"在手机上**长得一模一
  样**——都是没消息。

  心跳把"没消息"从两义变成一义：**收不到心跳 = 出事了。**

  每小时一条，内容含时间戳。家长只要养成"中午没看到心跳就查一下"的习惯，NFR-06（崩溃后
  1 小时内察觉）就达成了。

  ### 8.5 冷却机制

  报警后 10 分钟内不再重复报警。

  > **实现教训（值得写进简历讲）**：初版把"上次报警时刻"初始化为 `0.0`，冷却判断写成
  `当前时刻 - 0 < 600`。因为 `time.time()` 返回的是 17 亿这样的大数，这个条件**永远为假
  **，冷却逻辑实际从未生效——但功能表现"碰巧正确"，因为第一次报警时本来也不该冷却。
  >
  > **这类"靠巧合正确"的代码是典型技术债**：它今天能跑，但任何人改动时间基准就会炸。已
  改为初始化 `None` 并显式判空。

  ### 8.6 通知发送设计

  **🔴 级告警拆两条连发**（因为企业微信的 `image` 类型不支持 @所有人）：

  ```
  第 1 条（text，mentioned_list=["@all"]）：
    🔴 宝宝已经哭了 3 分钟，画面里没有人
    时间：2026-07-23 11:32:15
    建议打个电话回家看看情况。

  第 2 条（image）：
    [触发时刻的抓拍图]
  ```

  **发送保障**：
  - 失败重试 3 次，指数退避（1s / 4s / 16s）
  - 三次失败 → 写入待补发队列，网络恢复后补发，文案带「⚠️ 补发（原发生时间
  XX:XX）」前缀
  - **节流**：逼近 20 条 / 分钟上限时合并为一条汇总消息，不丢事件
  - **通知失败绝不导致主程序退出**（FR-07）

  ### 8.7 视觉感知设计（阶段 2–3）

  #### 8.7.1 成人与婴儿的区分

  一次检测输出画面中所有人的框与关键点。区分方法：

  ```
  成人判定 = 人体框高度 > 已标定床长的 1.2 倍
          或 检测框中心持续位于床区域之外且尺寸偏大
  ```

  **理由**：床的尺寸在配置时已标定，是画面中**唯一可靠的"尺子"**。婴儿躺在床里、成人站
  在床边，两者在画面中的尺度差异极大——用尺度判断比训练一个"成人 /
  婴儿"分类器**可靠得多，且零成本**。

  #### 8.7.2 俯卧与遮脸判定（R1 / R2 / R7）

  **核心思路：只看头，不看全身。** 盖被子后躯干关键点会大量丢失，但头一般露在外面。

  用头部 5 个关键点（鼻、左右眼、左右耳）的**可见性组合**判定朝向：

  | 可见关键点组合 | 判定 |
  |---|---|
  | 鼻 + 双眼 可见 | 仰卧（脸朝上）→ 正常 |
  | 鼻 + 单眼 + 单耳 | 侧卧 → 正常 |
  | 鼻、眼均不可见，双耳 / 头顶轮廓可见 | **俯卧（脸朝下）** → R1 计时 |
  | 头部区域整体检测不到，但躯干在床上 | **头被埋住** → R1 计时 |

  **遮脸（R2）**：以头部关键点外扩为 ROI，检测该 ROI 是否被"非人体物体"覆盖——与空床基线
  （§8.7.4）差分出现大面积新增区域，且不属于躯干连通区。

  > **夜视适配（关键）**：红外画面是灰度的，**不能用肤色做任何判断**。以上方案全部基于
  关键点几何和前景差分，不依赖颜色，**天生兼容夜视**。这是选择该方案而非颜色方案的决定
  性原因。

  **防抖**：关键点置信度 < 0.3 一律视为不可见；连续 3 帧（约 0.6
  秒）判定一致才更新状态。

  #### 8.7.3 越界判定（R4 / R5 / R8）

  一次性配置工具（FR-16）让用户在画面上框出两样东西：**床面区域**、**栏杆顶沿线**。

  | 规则 | 判定 |
  |---|---|
  | R4 婴儿离开画面 | 人体框与床区域交并比 < 0.2，持续 60 秒 |
  **为什么**：说不出"为什么是 0.25 而不是 0.3"，这个系统就不是工程，是玄学。

  ### 第九条：规则逻辑必须有自动化测试

  每条告警规则合并前必须有自动化测试，至少覆盖：该触发时触发、**不该触发时不触发**（这
  条更重要，见第二条）、边界条件（时长刚好卡在阈值上）。

  ### 第十条：成本有上限

  长期运行成本 ≤ **5 元 /
  月**。任何方案在采纳前必须估算长期成本，超限的必须改造或否决。

  ### 宪法修订记录

  | 版本 | 日期 | 变更 |
  |---|---|---|
  | 1.0.0 | 2026-07-23 | 初版 |
  | 1.0.1 | 2026-07-23 | 并入本文档 §2，内容未变 |

  ---

  ## 3. 干系人与使用场景

  ### 3.1 干系人

  | 角色 | 关注点 |
  |---|---|
  | **家长（主用户）** | 不在家时的安心感；不想被误报骚扰 |
  | **看护人（保姆 / 长辈）** | **知情权**——必须被告知有摄像头及其用途 |
  | **婴儿（约 6 个月）** | 隐私；不被 850nm 红外灯的可见红光影响睡眠 |

  ### 3.2 主场景：白天（**本系统的主要场景**）

  ```
  09:00  家长出门上班，看护人在家
  09:00  监护程序在家里的 Windows PC 上持续运行
  10:00  收到心跳："✅ 系统正常，一切安静"
  11:32  宝宝开始哭 → 持续 3 分钟，画面里始终没有大人
         → 🔴 微信推送 + 当时的抓拍图
  11:33  家长打电话给看护人；想看现场就打开摄像头自带的 App
  12:00  收到心跳
  ...
  18:30  家长回家，关掉程序
  ```

  ### 3.3 次要场景：夜间（**附赠，不做承诺**）
  | 代码里只保留占位符 | 提供 `config.example.py` 供他人参考 |
  | Claude API Key 同上 | 泄露会产生真实账单 |

  > 这是上传 GitHub 前的**硬性检查项**。密钥泄漏到公开仓库是最常见的新手事故，而且
  **Git 有历史记录——提交过再删掉是没用的，仍然能被翻出来。**

  ### 8.10 日志

  每秒一行
  CSV：时间戳、哭声分数、呜咽分数、开心分数、最像的类别、窗口比例、当前状态、已哭秒数。

  **用途**：调参依据、误报复盘、以及**"我不是拍脑袋定的阈值"的证据**（宪法第八条）。

  ---

  ## 9. 数据与隐私

  ### 9.1 原则与落地

  | 原则 | 落地 |
  |---|---|
  | 知情 | **必须告知看护人**摄像头存在及用途 |
  | 最小采集 | 镜头**只对准婴儿床**，不拍房间其他区域 |
  | 最小留存 | **仅保存触发时刻的画面**，不做连续录像，不存事件前后录像 |
  | 不出户 | 视频流不离开家庭网络 |
  | 密钥保护 | 见 §8.9 |

  ### 9.2 ⚠️ 哪些数据会离开你家（如实列清）

  "视频不出户"有**两条例外**，必须写明，不能含糊：

  | # | 什么数据 | 去哪 | 频率 | 为什么接受 |
  |---|---|---|---|---|
  | 例外 1 | 🔴 级告警的**一张抓拍图** | 腾讯服务器（企业微信） | 每次红色告警 1 张 |
  一眼看清现场，省掉一次往返。用户已知情并同意 |
  | 例外 2 | 可疑时刻的 **2–3 帧**（裁剪到床区域） | **Anthropic 服务器（境外）** |
  每次可疑触发 | 换取误报率大幅下降（宪法第二条）。用户已知情并同意 |

  **除以上两项外，任何图像、视频、音频都不离开家庭网络。**

  > **例外 2 需要特别注意**：这些帧会传输到**中国境外**。如果将来这个项目要送给别人用、
  或者公开发布，这一点必须在显著位置告知，让对方自己决定是否开启第二层。

  ### 9.3 本地留存

  | 数据 | 策略 |
  |---|---|
  | CSV 打分日志 | 长期保留（体积小，是调参的唯一依据） |
  | 触发时刻抓拍图 | 保留 30 天后自动清理 |
  | 连续录像 | **不产生**（宪法第六条） |

  ---

  ## 10. 部署与运行环境

  ### 10.1 运行环境

  | 项 | 内容 |
  |---|---|
  | **主机** | **家中已有的 Windows PC**（不购置新硬件，ADR-01） |
  | Python | 3.10+ |
  | 依赖 | `mediapipe`、`numpy`、`sounddevice`；阶段 2 起加 `opencv-python` |
  | 国内加速 | pip 使用清华镜像 `https://pypi.tuna.tsinghua.edu.cn/simple` |
  | 模型分发 | `yamnet.tflite` **随包分发**（托管在 Google Storage，国内无法直接下载）
  |
  | ⚠️ 注意 | 必须**关闭 Windows 电源计划的休眠**，否则电脑睡了程序就停了 |

  ### 10.2 摄像头要求

  | 指标 | 要求 | 当前状态 | 理由 |
  |---|---|---|---|
  | **协议** | 必须支持 **RTSP 或 ONVIF** | ✅ **已确认支持** | 硬性要求。只能用厂商
  App 的摄像头，程序读不到画面 |
  | **分辨率** | ≥ 2K | ✅ **2K** | **分辨率决定能否看清小物件**（§8.7.4）。2K 下 1.5cm
  物体 ≈ 26 像素，勉强够 |
  | **红外波长** | **940nm**，不要 850nm | ⬜ **待确认型号后核实** | 850nm
  会发出可见的暗红光，影响婴儿睡眠；940nm 完全不可见 |
  | 麦克风 | 有无均可 | — | 阶段 1 用的是 PC 麦克风，不依赖摄像头音频 |
  | 视角 | 覆盖整张婴儿床 + 床边一圈 | ⬜ 安装时确认 |
  视角决定覆盖范围，**分辨率决定小物件可检测性**，两者是不同的事 |
  | 安装 | 固定安装，位置不可随意移动 | ⬜ |
  摄像头一移动，床区域标定就失效，必须重新标定 |

  > **⬜ 待办**：摄像头具体型号未记录。需确认红外波长是 940nm 还是 850nm——如果是
  850nm，夜间会有可见红光，虽不影响功能但可能影响宝宝睡眠。

  ### 10.3 分发方式（NFR-08）

  Windows 批处理封装，非技术用户**双击即可，不需要敲命令行**：

  | 文件 | 作用 |
  |---|---|
  | `0-一键安装.bat` | 建虚拟环境、装依赖（走清华镜像） |
  | `1-查看麦克风.bat` | 列出所有麦克风及编号 |
  | `2-测试.bat` | 实时显示打分，用于调阈值，**不报警** |
  | `3-正式运行.bat` | 正式运行 |
  | `4-标定床区域.bat` | （阶段 3）弹出画面，鼠标拖框标定床与栏杆 |

  ---

  ## 11. 测试策略

  ### 11.1 已实现的测试（阶段 1，**6 / 6 通过**）

  | # | 测试 | 验证什么 |
  |---|---|---|
  | 1 | 短暂哭 20 秒 | 不该报警 → **防误报**（宪法第二条） |
  | 2 | 持续哭 5 分钟 | 该报 1 次 → 主功能 |
  | 3 | 哭中停顿换气 | 不该判成"停止哭" → **迟滞有效性** |
  | 4 | 连续哭 20 分钟 | 只报 2 次 → **冷却有效性** |
  | 5 | 白噪声 / 静音打分 | 分数 0.0000 → 不误判 |
  | 6 | webhook 未配置 | 返回 False 不崩溃 → 容错 |

  ### 11.2 尚未覆盖（技术债，需补）

  - 真实婴儿哭声录音的**召回率**测试
  - **白噪音机开启状态下**的哭声召回率（§8.3，最高优先级）
  - 长时间运行的内存泄漏 / 稳定性
  - 断网时的推送重试与补发
  - 摄像头断流恢复
  - 视觉规则的自动化测试（宪法第九条要求，阶段 2 起必须同步补）

  ### 11.3 实地调参（阶段 5）

  连续运行**一周**，用 CSV 日志复盘每一次误报与漏报，逐步调整 `CRY_THRESHOLD`，目标
  NFR-01（< 1 次 / 晚）。

  **必测项**：白噪音机开着时的真实哭声打分分布（§8.3）。

  ### 11.4 视觉部分的验收（阶段 3）

  | 项 | 标准 |
  |---|---|
  | R1 俯卧判定召回率 | ≥ 95%（可用婴儿仿真娃娃摆拍补充样本，避免刻意制造危险场景） |
  | R2 遮脸判定召回率 | ≥ 90% |
  | 成人检测 | 成人入画识别率 ≥ 95%，不把婴儿误判为成人 |
  | 夜视有效性 | **以上指标在红外黑白画面下同样成立**（主场景虽是白天，但夜间也会运行）
  |
  | 整机误报 | **连续 7 个真实日夜，人工复核每条告警，日均 ≤ 1 次** |

  ---

  ## 12. 风险与已知限制

  **为什么**：说不出"为什么是 0.25 而不是 0.3"，这个系统就不是工程，是玄学。

  ### 第九条：规则逻辑必须有自动化测试

  每条告警规则合并前必须有自动化测试，至少覆盖：该触发时触发、**不该触发时不触发**（这
  条更重要，见第二条）、边界条件（时长刚好卡在阈值上）。

  ### 第十条：成本有上限

  长期运行成本 ≤ **5 元 /
  月**。任何方案在采纳前必须估算长期成本，超限的必须改造或否决。

  ### 宪法修订记录

  | 版本 | 日期 | 变更 |
  |---|---|---|
  | 1.0.0 | 2026-07-23 | 初版 |
  | 1.0.1 | 2026-07-23 | 并入本文档 §2，内容未变 |

  ---

  ## 3. 干系人与使用场景

  ### 3.1 干系人

  | 角色 | 关注点 |
  |---|---|
  | **家长（主用户）** | 不在家时的安心感；不想被误报骚扰 |
  | **看护人（保姆 / 长辈）** | **知情权**——必须被告知有摄像头及其用途 |
  | **婴儿（约 6 个月）** | 隐私；不被 850nm 红外灯的可见红光影响睡眠 |

  ### 3.2 主场景：白天（**本系统的主要场景**）

  ```
  09:00  家长出门上班，看护人在家
  09:00  监护程序在家里的 Windows PC 上持续运行
  10:00  收到心跳："✅ 系统正常，一切安静"
  11:32  宝宝开始哭 → 持续 3 分钟，画面里始终没有大人
         → 🔴 微信推送 + 当时的抓拍图
  11:33  家长打电话给看护人；想看现场就打开摄像头自带的 App
  12:00  收到心跳
  ...
  18:30  家长回家，关掉程序
  ```

  ### 3.3 次要场景：夜间（**附赠，不做承诺**）
  | **白噪音淹没哭声** | 实地调参 + CSV 复盘（§8.3）。**当前最主要的漏报风险** |
  | 通用姿态模型对婴儿效果下降 | 婴儿身体比例与成人差异大，且常被被子遮挡。阶段 3
  开工前必须先用真实录像验证头部关键点可用率 ≥ 85%，不通过则改用头部专用检测器 |
  | Claude API 境外访问不稳定 | 失败即降级为直接推送，不阻塞主链路 |

  ---

  ## 13. 路线图与任务拆解

  | 阶段 | 内容 | 状态 |
  |---|---|---|
  | **1** | 哭声检测 + 微信推送 + 心跳 + CSV 日志 | ✅ **已完成，6/6 测试通过** |
  | **2** | 摄像头接入（RTSP）+ 断流重连 + 画面异常检测 + 成人检测 → 规则 R3
  联合判定、R9、R10 | ⬜ 下一步 |
  | **3** | 婴儿姿态（俯卧 / 遮脸 / 出框）+ 越界（R5/R8）+ 床区域标定工具 + 抓拍 →
  R1/R2/R4/R5/R7/R8 | ⬜ |
  | **4** | Claude API 二次确认 | ⬜ |
  | **5** | **一周实地调参**，达成误报 < 1 次 / 晚（含白噪音场景实测） | ⬜ |
  | **6** | 空床基线 + 床内异物白名单扫描 → R6 | ⬜ |

  ### 13.1 阶段 2 的前置条件（开工前必须做完）

  | # | 事项 | 完成标准 |
  |---|---|---|
  | P-1 | 记录摄像头型号，核实红外波长 | 确认是 940nm 还是 850nm |
  | P-2 | 拿到 RTSP 地址并实测拉流 | 能连续拉流 30 分钟不断，且能读出实际分辨率与帧率 |
  | P-3 | 摄像头固定安装到位 | 覆盖整张床 + 床边一圈，安装后不再移动 |
  | P-4 | 告知看护人摄像头存在及用途 | 宪法第六条要求 |

  ### 13.2 阶段 3 的卡点验证

  用真实录像（白天 + 夜视各 30 段）验证头部关键点可用率 ≥ 85%。**不通过就不进入阶段 3
  开发**，改走头部专用检测器路线。带着不确定性往下写代码是这个项目最大的返工风险。

  ---

  ## 14. 设计决策记录（ADR）

  > **这一章是面试时要讲的内容。**
  每条记录都是"我面对什么矛盾、有哪些选项、我选了哪个、为什么"。
  > 所有决策日期均为 2026-07-23。

  | # | 决策 | 备选方案 | 为什么这么选 |
  |---|---|---|---|
  | **ADR-01** | **跑在家里现有的 Windows PC 上** | 买 N100 迷你主机（¥900–1400）/
  树莓派 5 | 家里已有 PC，算力足够。买新硬件既违反"不增加成本"，也让非技术用户多一道装
  机门槛（NFR-08）。**能不引入的复杂度就不引入。** |
  | **ADR-02** | **主场景定为白天（家长上班、看护人在家）** | 夜间为主 / 日夜同等 |
  用户实际痛点是"我不在，宝宝哭了有没有人管"。定为白天后，"微信穿不透静音"从致命缺陷降
  级为可接受限制（§12.3），整个通知方案得以成立 |
  | **ADR-03** | **哭声告警必须是联合规则：哭 3 分钟 + 全程无成人** | 哭 1 分钟就报 /
  哭就报 |
  白天有人带，宝宝哭一分钟完全正常。**单纯"哭"不是异常，"哭了没人管"才是异常。**
  这一条把误报率降了一个数量级，是宪法第二条的直接产物 |
  | **ADR-04** | **通知走企业微信群机器人** | 自建服务器 / 微信小程序 / Bark / ntfy |
  纯出站 HTTPS，不需要公网 IP、端口映射、域名、备案（NFR-05）；免费；iOS+Android
  双端；家人用普通微信扫码进群即可。**代价是穿不透勿扰模式，已接受** |
  | **ADR-05** | **两层级联：本地筛查 + Claude API 复核** | 全程调 API / 纯本地 |
  全程调 API 每月数千元（破宪法第十条）；纯本地误报多到没法用（破宪法第二条）。**级联是
  同时满足这两条的唯一结构。** 本地挡掉 99% 的平静时间，API 只在 1% 的可疑时刻被唤醒 |
  | **ADR-06** | **不做网页端 / 直播 / 手机 App** | 自研 WebRTC 直播 + 内网穿透（约 12
  天工作量） | 用户明确表示：收到报警看一眼照片就够，想看现场直接开摄像头自带的
  App。**摄像头厂商已经把这件事做好了，重做一遍只会多一个对外的口子（违反
  NFR-05）和多一份维护负担。** 省下 12 天工作量 |
  | **ADR-07** | **异物检测在"东西出现在床上"时就报，不等它送到嘴边** | 检测"手往嘴边送
  + 床上有异物" | 后者是"急救"，而急救链路必然赶不上窒息窗口（§12.2）——那是**假防御**
  ，违反宪法第五条。前者是"预防性巡检"，东西还没进嘴，推送延迟完全来得及 |
  | **ADR-08** | **只保存触发时刻的单帧，不存事件前后录像** | 存事发前 10 秒 + 后 10
  秒短视频 | 宪法第六条"仅保存触发时刻的画面"。短视频虽然便于复盘，但它是"连续录像"的滑
  坡起点，且抓拍图 + CSV 日志已足够复盘。**隐私红线不为便利让步** |
  | **ADR-09** | **AI 复核用境外 Claude API** | 国内大模型（通义/豆包/智谱）/ 不用 |
  用户明确选择 Claude。**代价是宝宝的照片会传输到境外**，已在 §9.2 例外 2
  显式记录。若将来分享给他人使用，必须在显著位置告知 |
  | **ADR-10** | **报警消息附带抓拍图** | 只发文字 | 一眼看清现场，省掉一次"打开 App
  确认"的往返。代价是图片经腾讯服务器（§9.2 例外 1），已接受。**保留一键关闭开关** |
  | **ADR-11** | **新增 R5（翻越）/ R8（卡栏杆）两条规则** | 沿用原设计"6
  个月不会爬，不做" | 6 个月翻越栏杆确实几乎不可能，但**手脚卡栏杆是真实风险**，且翻越
  能力几个月内就会出现——现在做等于提前投资。R8
  误报风险高，单独监控其误报次数，超标即下线（§12.4） |
  **为什么**：说不出"为什么是 0.25 而不是 0.3"，这个系统就不是工程，是玄学。

  ### 第九条：规则逻辑必须有自动化测试

  每条告警规则合并前必须有自动化测试，至少覆盖：该触发时触发、**不该触发时不触发**（这
  条更重要，见第二条）、边界条件（时长刚好卡在阈值上）。

  ### 第十条：成本有上限

  长期运行成本 ≤ **5 元 /
  月**。任何方案在采纳前必须估算长期成本，超限的必须改造或否决。

  ### 宪法修订记录

  | 版本 | 日期 | 变更 |
  |---|---|---|
  | 1.0.0 | 2026-07-23 | 初版 |
  | 1.0.1 | 2026-07-23 | 并入本文档 §2，内容未变 |

  ---

  ## 3. 干系人与使用场景

  ### 3.1 干系人

  | 角色 | 关注点 |
  |---|---|
  | **家长（主用户）** | 不在家时的安心感；不想被误报骚扰 |
  | **看护人（保姆 / 长辈）** | **知情权**——必须被告知有摄像头及其用途 |
  | **婴儿（约 6 个月）** | 隐私；不被 850nm 红外灯的可见红光影响睡眠 |

  ### 3.2 主场景：白天（**本系统的主要场景**）

  ```
  09:00  家长出门上班，看护人在家
  09:00  监护程序在家里的 Windows PC 上持续运行
  10:00  收到心跳："✅ 系统正常，一切安静"
  11:32  宝宝开始哭 → 持续 3 分钟，画面里始终没有大人
         → 🔴 微信推送 + 当时的抓拍图
  11:33  家长打电话给看护人；想看现场就打开摄像头自带的 App
  12:00  收到心跳
  ...
  18:30  家长回家，关掉程序
  ```

  ### 3.3 次要场景：夜间（**附赠，不做承诺**）
  | ACLab CribNet | 婴儿床场景 | 非商用 | ✅（仅自用） |
  | SyRIP / MINI-RGBD | 婴儿数据集 | 非商用 | ✅（仅自用） |
  | AggPose | 婴儿姿态 | 非商用 | ✅（仅自用） |
  | ultralytics YOLO | 目标检测 | **AGPL-3.0** | ⚠️ 自用可，分发需谨慎 |

  ### 15.3 企业微信群机器人怎么弄（照着做即可）

  1. 手机应用商店下载 **企业微信**，用微信扫码登录，选「创建企业」，规模选最小的，名称
  随便填（比如"家庭"）。
  2. 在企业微信里**新建一个群聊**（至少 3 人）。
  3. 家人不用装企业微信——群设置里选「**邀请微信用户加入**」生成二维码，家人用**普通微信

  ### 第九条：规则逻辑必须有自动化测试

  每条告警规则合并前必须有自动化测试，至少覆盖：该触发时触发、**不该触发时不触发**（这
  条更重要，见第二条）、边界条件（时长刚好卡在阈值上）。

  ### 第十条：成本有上限

  长期运行成本 ≤ **5 元 /
  月**。任何方案在采纳前必须估算长期成本，超限的必须改造或否决。

  ### 宪法修订记录

  | 版本 | 日期 | 变更 |
  |---|---|---|
  | 1.0.0 | 2026-07-23 | 初版 |
  | 1.0.1 | 2026-07-23 | 并入本文档 §2，内容未变 |

  ---

  ## 3. 干系人与使用场景

  ### 3.1 干系人

  | 角色 | 关注点 |
  |---|---|
  | **家长（主用户）** | 不在家时的安心感；不想被误报骚扰 |
  | **看护人（保姆 / 长辈）** | **知情权**——必须被告知有摄像头及其用途 |
  | **婴儿（约 6 个月）** | 隐私；不被 850nm 红外灯的可见红光影响睡眠 |

  ### 3.2 主场景：白天（**本系统的主要场景**）

  ```
  09:00  家长出门上班，看护人在家
  09:00  监护程序在家里的 Windows PC 上持续运行
  10:00  收到心跳："✅ 系统正常，一切安静"
  11:32  宝宝开始哭 → 持续 3 分钟，画面里始终没有大人
         → 🔴 微信推送 + 当时的抓拍图
  11:33  家长打电话给看护人；想看现场就打开摄像头自带的 App
  12:00  收到心跳
  ...
  18:30  家长回家，关掉程序
  ```

  ### 3.3 次要场景：夜间（**附赠，不做承诺**）

  程序可以整夜运行、规则照常生效，但**本系统不承诺能在夜里叫醒你**——微信消息穿不透手机
  静音与勿扰模式（见
  §12.3）。夜间价值主要在于**第二天早上能回看日志**，以及万一你醒着就能收到。

  ### 3.4 关键约束

  - **家长不在现场**，因此"系统静默"和"一切正常"看起来完全一样 → **心跳是刚需**
  - 婴儿约 6 个月，主要在婴儿床内，摄像头视野固定、距离可控
  - 双端（iOS + Android）都要能收到
  - **宝宝房间常年开着白噪音机 / 加湿器 / 空气净化器** →
  持续底噪会淹没哭声，**这是本项目最主要的漏报风险来源**（见 §8.3、§11.3）
  - 家里**没有宠物、没有更大的孩子** → 哭声误报源少，灵敏度可适当调低以补偿底噪

  ---

  ## 4. 需求

  ### 4.1 功能需求

  | 编号 | 需求 | 阶段 |
  |---|---|---|
  | FR-01 | 持续采集麦克风音频，识别婴儿哭声 | 1 ✅ |
  | FR-02 | 哭声**持续**超过阈值时推送告警 | 1 ✅ |
  | FR-03 | 短暂的哭声中断（换气）**不得**重置持续计时 | 1 ✅ |
  | FR-04 | 告警后有静默期，避免刷屏 | 1 ✅ |
  | FR-05 | 定时心跳，自证存活 | 1 ✅ |
  | FR-06 | 全量打分写入 CSV 日志，供后续调参与复盘 | 1 ✅ |
  | FR-07 | 通知渠道未配置或故障时**不崩溃**，继续工作 | 1 ✅ |
  | FR-08 | 所有可调参数集中在一处，非技术用户可修改 | 1 ✅ |
  | FR-09 | 采集摄像头画面（RTSP） | 2 |
  | FR-10 | 检测画面中的成人（人数统计） | 2 |
  | FR-11 | 联合规则："哭 + 全程无成人" → 升级告警 | 2 |
  | FR-12 | 断流自动重连；画面异常（遮挡/全黑/卡死）识别 | 2 |
  | FR-13 | 婴儿姿态识别（俯卧 / 脸遮挡 / 出框） | 3 |
  | FR-14 | 触发时抓拍，随告警发送图片 | 3 |
  | FR-15 | 越界检测（正在翻越床栏 / 手脚卡栏杆） | 3 |
  | FR-16 | 一次性配置工具：在画面上框出床区域与栏杆顶沿 | 3 |
  | FR-17 | 大模型 API 二次确认，抑制误报 | 4 |
  | FR-18 | 婴儿床内陌生物件检测（白名单外） | 6 |
  | FR-19 | 空床基线学习（用于 FR-18） | 6 |

  ### 4.2 非功能需求

  | 编号 | 需求 | 指标 |
  |---|---|---|
  | NFR-01 | 误报率 | **< 1 次 / 晚**（第一优先） |
  | NFR-02 | 告警端到端延迟 | < 15 秒 |
  | NFR-03 | CPU 占用 | 音频层单核 < 5% |
  | NFR-04 | 运行成本 | ≤ 5 元 / 月 |
  | NFR-05 | **网络要求** | **仅需出站 HTTPS**。不需要公网
  IP、不需要端口映射、不需要域名、不需要备案 |
  | NFR-06 | 崩溃可见性 | 程序死亡后 ≤ 1 小时内家长察觉 |
  | NFR-07 | 隐私 | 视频默认不出家门；仅保存触发时刻画面 |
  | NFR-08 | 易用性 | 非技术用户从安装到跑起来 ≤ 30 分钟，**不需要敲命令行** |
  | NFR-09 | 稳定性 | 连续运行 ≥ 12 小时不中断 |

  > **NFR-05 是一条硬约束，它排除了一整类设计。** 任何需要"从外网访问家里"的方案（网页
  端、直播服务、内网穿透）都违反它，因此全部不做（NG6 / ADR-06）。

  ---

  ## 5. 告警规则表（核心业务逻辑）

  这是整个系统的核心。规则的取舍针对"约 6 个月 + 白天家长不在家"这个具体场景。

  | # | 级别 | 触发条件 | 说明 | 阶段 |
  |---|---|---|---|---|
  | **R1** | 🔴 | **俯卧 且 脸不可见 > 30 秒** | 最高优先级：刚学会翻身但翻不回去 | 3 |
  | **R2** | 🔴 | **脸被遮挡（被子/毛巾）> 20 秒** | | 3 |
  | **R3** | 🔴 | **哭 > 3 分钟 且 全程画面内无成人** | 音频 + 视觉联合判断 |
  1（音频）/ 2（联合） |
  | **R4** | 🔴 | **婴儿离开画面 > 60 秒** | 覆盖：掉下床 / 被抱走 / 镜头被挡 | 3 |
  | **R5** | 🔴 | **身体越过床栏顶沿 > 3 秒** | 正在往外翻。6
  个月用不上，会扶站后是救命规则 | 3 |
  | **R6** | 🟡 | 婴儿床内出现白名单外的物体 > 30 秒 | 误食**前置**防御，见 §12.2 | 6 |
  | **R7** | 🟡 | 翻身成俯卧（脸仍可见）> 30 秒 | 预警，未达红色 | 3 |
  | **R8** | 🟡 | 手或脚伸出床栏外 且 20 秒内几乎不动 |
  疑似卡住。**误报风险最高的一条**，见 §12.4 | 3 |
  | **R9** | 🟡 | 画面内 > 2 小时无成人 | 看护人长时间离岗 | 2 |
  | **R10** | 🟢 | 每 2 小时状态摘要 | | 2 |
  | **R11** | ⚙️ | **每 1 小时心跳** | 系统存活证明（宪法第四条） | 1 ✅ |

  **推送形式**：🔴 级发送**文字 + 抓拍图**；🟡 🟢 ⚙️ 仅文字。

  ### 5.1 设计要点

  - **R3 是联合规则，单纯"哭"不告警。** 宝宝哭而有人在管，是正常育儿场景，报了就是误报
  。这是本系统与市面上"哭了就响"的产品最大的区别。
  - **所有规则都有时间阈值**（宪法第三条），没有任何瞬时触发。
  - **R6
  是预防性的，不是急救性的**（§12.2）。它在东西还没进嘴时就报，而不是等到宝宝往嘴里塞。
  - **R5 / R8 是新增的**，原设计假设"6
  个月不会爬"因而未覆盖。实际上手脚卡栏杆是真实风险，而翻越能力会在几个月内出现。

  ### 5.2 抑制与去重

  | 编号 | 规则 | 理由 |
  |---|---|---|
  | S-1 | **有成人在画面内时，R1/R2/R6/R7/R8 全部不触发**；成人离开 30 秒后恢复 |
  大人在旁边换尿布、抱起宝宝时不该响 |
  | S-2 | 同一规则触发后 **10 分钟**冷却，期间不重复推送 | 防止刷屏（宪法第二条） |
  | S-3 | **画面异常时（遮挡 / 全黑 / 卡死）停止全部视觉规则**，改发一条
  🟡「摄像头看不清了」 | 拿噪声当行为判断是最典型的误报来源 |
  | S-4 | 断网时事件照常落本地日志，网络恢复后补发并标注「补发」 | |

  ---

  ## 6. 系统架构

  ### 6.1 总体：两层级联

  ```
  ┌──────────────────────────────────────────────────────────┐
  │  第 1 层：本地常驻筛查（免费、7×24、低精度）                 │
  │                                                          │
  │   麦克风 ──► YAMNet 音频分类 ──┐                           │
  │                              ├──► 规则引擎（§5）           │
  │   摄像头 ──► 检测 / 姿态 ─────┘         │                  │
  │   (RTSP)                              │                  │
  └───────────────────────────────────────┼──────────────────┘
                                          │ 仅在"可疑"时唤醒
                                          ▼
  ┌──────────────────────────────────────────────────────────┐
  │  第 2 层：大模型 API 复核（收费、按次、高精度）               │
  │   抓 2–3 帧 ──► Claude API ──► 确认 / 否决                 │
  └───────────────────────────────────────┬──────────────────┘
                                          │ 确认后
                                          ▼
                              企业微信群机器人 ──► 家人手机微信
                                          │
                                          └─► 想看现场？
                                              打开摄像头自带 App
  ```

  ### 6.2 为什么分两层

  | 方案 | 精度 | 成本 | 结论 |
  |---|---|---|---|
  | 全程调大模型 API | 高 | 7×24 每秒一帧 ≈ **每月数千元** | ❌ 违反宪法第十条 |
  | 只用本地小模型 | 低 | 免费，但**误报多到没法用** | ❌ 违反宪法第二条 |
  | **两层级联** | **高** | **每月几元** | ✅ |

  本地层负责**把 99% 的平静时间挡掉**，大模型只在剩下 1% 的可疑时刻被唤醒。

  > **这是成本工程，不是技术炫技。** 宪法第二条（误报是生死线）和第十条（成本 ≤ 5
  元/月）单独看都做不到，两层级联是同时满足它们的唯一结构。

  ### 6.3 组件划分

  | 组件 | 职责 | 状态 |
  |---|---|---|
  | `audio_watcher` | 麦克风采集 + 哭声打分 | ✅ 阶段 1 |
  | `cry_state_machine` | 迟滞状态机，输出"在哭 / 不哭" | ✅ 阶段 1 |
  | `notifier` | 企业微信推送（文字 / 图片） | ✅ 阶段 1（图片待补） |
  | `heartbeat` | 定时存活消息 | ✅ 阶段 1 |
  | `logger` | 全量打分落 CSV | ✅ 阶段 1 |
  | `video_watcher` | RTSP 采集、断流重连、画面异常检测 | ⬜ 阶段 2 |
  | `person_detector` | 成人检测 / 计数、成人与婴儿区分 | ⬜ 阶段 2 |
  | `pose_estimator` | 婴儿姿态、头部朝向 | ⬜ 阶段 3 |
  | `zone_config` | 一次性配置工具：标定床区域与栏杆顶沿 | ⬜ 阶段 3 |
  | `rule_engine` | 多模态规则融合、抑制、冷却 | ⬜ 阶段 3 |
  | `llm_verifier` | 大模型二次确认 | ⬜ 阶段 4 |
  | `object_scanner` | 空床基线 + 床内异物扫描 | ⬜ 阶段 6 |

  ### 6.4 数据流（阶段 1，已实现）

  ```
  麦克风
    │ 16kHz 单声道
    ▼
  1 秒切片 ──► YAMNet ──► 521 类得分
                            │ 取 Baby cry / Crying,sobbing 的最大值
                            ▼
                      哭声分数 (0.0 ~ 1.0)
                            │ ≥ CRY_THRESHOLD(0.25) → 记 1，否则记 0
                            ▼
                    最近 30 秒滑动窗口 → 计算 1 的比例
                            ▼
                      迟滞状态机（§8.1）
                      进入: 比例 ≥ 45% → "在哭"
                      退出: 比例 ≤ 20% → "不哭"
                            ▼
                连续在哭 ≥ 180 秒 且 不在冷却期
                            ▼
                      企业微信推送 → 冷却 600 秒
  ```

  ---

  ## 7. 技术选型与理由

  ### 7.1 音频识别：YAMNet

  | 项 | 内容 |
  |---|---|
  | 选型 | **YAMNet**（Google，AudioSet 训练，521 类） |
  | 运行框架 | MediaPipe Tasks Audio（`AudioClassifier`） |
  | 许可证 | **Apache-2.0** ✅ |
  | 模型大小 | 4.1 MB |
  | 用到的类 | `Baby cry, infant cry`(167)、`Crying,
  sobbing`(327)、`Whimper`(359)、`Baby laughter`(515)、`Speech`(13) |
  | 实测性能 | 1 秒音频分类耗时 **5 ms**（约 200× 实时），CPU 占用可忽略 |

  **为什么选它**：Apache 许可干净、体积小、CPU 就能跑、类别里**直接有"婴儿哭"**、Google
  长期维护。

  ### 7.2 视觉：分层选型

  | 用途 | 选型 | 许可证 |
  |---|---|---|
  | 成人检测 / 计数 | RT-DETR / RF-DETR | Apache-2.0 ✅ |
  | 通用姿态 | MediaPipe Pose | Apache-2.0 ✅ |
  | **婴儿专用姿态** | ACLab 系列（Infant-Pose-Estimation、CribNet、SyRIP、AggPose） |
  ⚠️ 非商用 |

  > **许可证关键判断**：几乎**所有**婴儿专用视觉模型都是非商用或
  AGPL。因为本项目明确为自用 + 送朋友（NG7），这些限制不适用，可以放心用。
  > **一旦要商业化，整条视觉链必须重做。** 这一点必须写在 README 里。

  ### 7.3 推送通道：企业微信群机器人

  | 项 | 内容 |
  |---|---|
  | 接口 | `POST https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=KEY` |
  | 文字 | `{"msgtype":"text","text":{"content":"..."}}` |
  | 图片 | `{"msgtype":"image","image":{"base64":"...","md5":"..."}}`，**上限 2 MB** |
  | **限流** | **20 条 / 分钟**（需节流，见 §8.6） |
  | ⚠️ 约束 | **`image` 类型不支持 @所有人** → 🔴
  级告警必须拆成两条连发（先文字后图片） |
  | ⚠️ 约束 | `news` 图文卡片的封面图需**公网可访问 URL** → 与 NFR-05
  冲突，**不可用，已放弃** |
  | 费用 | 个人可用，无需营业执照，**免费** |
  | 双端 | iOS + Android 均支持 ✅ |

  **为什么它比自建服务器好**：**纯出站请求**。不需要公网 IP、不需要端口映射、不需要域名
  、不需要备案——家里的电脑只管往外发，路由器一行配置都不用改（NFR-05）。

  **额外优势**：企业微信在国产 ROM 上普遍在推送白名单里，不容易被系统杀掉后台。

  ### 7.4 视频接入与开发环境（2026-07-30 补记，随阶段 2 开工确定）

  | 项 | 选型 | 为什么 |
  |---|---|---|
  | 取流协议 | **RTSP**（摄像头行业标准） | 买摄像头时即按"支持 RTSP"选型，不锁死在厂商
  App 里；已实测可用（VLC 验证，2026-07-30） |
  | 视频读取/处理 | **OpenCV**（`opencv-python`，实装 5.0.0） |
  全球使用最广的视觉库，C++ 核心性能好；阶段 2/3
  的画面检测都在它上面做，接流与处理一个工具全包。备选"FFmpeg
  命令行拉流"被否：拉完还要倒手进 Python，多一道工序 |
  | 语言/运行时 | **Python 3.14.5**（Windows，`py` 启动器） | 已实测 mediapipe +
  opencv-python 在 3.14 均可安装导入 |
  | 依赖隔离 | **venv 虚拟环境** + requirements.txt |
  每项目独立"药箱"；保证换机可重建（NFR-08 的工程基础） |
  | 流连通性测试工具 | **VLC** |
  写代码前先用现成播放器验证水龙头是通的，把"流不通"和"代码不对"两类问题分开排查 |

  ### 7.5 被否决的方案（含理由）

  > 这一节比"选了什么"更重要。**面试时被问"你为什么不用 X"，答案在这里。**

  | 方案 | 否决理由 |
  |---|---|
  | `donateacry-corpus` 哭声原因分类 | 2020
  年后停止维护；**哭声原因分类学术上不成立**——只有"哭 / 不哭"是可靠的 |
  | `foduucom/baby-cry-classification` | 作者自报准确率 **38.5%**，比抛硬币还差 |
  | `codeperfectplus/AI-Baby-Monitor` | **许可证洗白陷阱**：仓库标 MIT，但代码 `import
  ultralytics`（AGPL-3.0）。**仓库的许可证不能给它引入的权重重新授权** |
  | 微信小程序推送 | 需要认证主体 + 自建服务器 + 备案，成本远高于收益 |
  | 卧室音响 / 蜂鸣器现场提示 | 主场景是家长不在家，现场提示无意义 |
  | Bark / ntfy 推送 | **唯一能穿透手机勿扰模式的方案**，但用户明确只要微信。代价见
  §12.3 |
  | 树莓派 5 / N100 迷你主机 | 家里已有 Windows
  PC，算力足够。**买新硬件违反"不增加成本"原则**（ADR-01） |
  | Docker / Linux 部署 | 目标用户是非技术背景，双击 `.bat`
  才是正确的分发方式（NFR-08） |
  | 网页端 + WebRTC 直播 + 内网穿透 | 违反 NFR-05；且摄像头自带 App
  已提供直播（ADR-06） |
  | 24 小时连续录像 | 隐私红线（宪法第六条） |
  | "无动作 = 无呼吸"告警 | 科学上错误（NG2） |
  | 事件前后各 10 秒短视频 | 违反宪法第六条"仅保存触发时刻的画面"（ADR-08） |

  ---

  ## 8. 详细设计

  ### 8.1 迟滞状态机（Hysteresis）—— 阶段 1 核心

  **问题**：宝宝哭的时候会停下来换气。如果用单一阈值判断，状态会在"哭 /
  不哭"之间疯狂反复横跳，计时器不断被重置，**永远攒不够 3 分钟**。

  **方案**：**进入和退出用两个不同的阈值**。

  ```
  比例 ≥ 45%  ──► 进入"在哭"
  比例 ≤ 20%  ──► 退出"在哭"
  20% ~ 45%   ──► 保持当前状态不变   ← 这段就是"迟滞带"
  ```

  换气造成的短暂下降（比例掉到 30%）落在迟滞带内，不会触发退出。

  > **类比**：空调设定 26℃，不会到 26.0℃ 就停、25.9℃ 就开，而是 27℃ 开、25℃ 停。中间那
  2 度就是迟滞带——防止压缩机反复启停烧坏。

  ### 8.2 关键参数

  | 参数 | 值 | 理由 |
  |---|---|---|
  | `SAMPLE_RATE` | 16000 Hz | YAMNet 硬性要求 |
  | `CHUNK_SECONDS` | 1.0 | 分类粒度 |
  | `SMOOTH_WINDOW` | 30 秒 | 平滑窗口，覆盖一个完整的哭—换气周期 |
  | `ENTER_RATIO` | 0.45 | 进入阈值 |
  | `EXIT_RATIO` | 0.20 | 退出阈值，与进入值拉开形成迟滞带 |
  | `CRY_THRESHOLD` | **0.25** | 单帧判定为哭的分数线，**⚠️ 唯一必须实地调的参数** |
  | `CRY_ALERT_SECONDS` | 180 | 哭满 3 分钟才报警 |
  | `ALERT_COOLDOWN_SECONDS` | 600 | 报警后 10 分钟静默 |
  | `HEARTBEAT_SECONDS` | 3600 | 每小时心跳 |

  > **宪法第八条要求**：以上除 `SAMPLE_RATE`
  外全部为**暂定值**，必须经一周实地数据验证后定稿。程序把每一秒的打分写进
  CSV，就是为了让你用真实数据回头调它们。

  ### 8.3 ⚠️ 白噪音机对阈值的影响（本项目最主要的漏报风险）

  宝宝房间常年开着白噪音机 / 加湿器 / 空气净化器。这带来一个**方向与直觉相反**的问题：

  - **白噪音本身不会被误判为哭。**（阶段 1 测试已验证：白噪声打分 0.0000）
  - **但它会淹没哭声。** 持续底噪拉低信噪比，YAMNet
  对哭声的打分整体下移，可能长期够不着 0.25 这条线 → **漏报**。

  **应对**：
  1. 实地录制"白噪音机开着 + 宝宝真实哭声"的音频，看实际打分分布
  2. 根据分布**下调** `CRY_THRESHOLD`
  3.
  家里**没有宠物、没有更大的孩子**（§3.4），哭声误报源本来就少，因此下调阈值的代价可控
  4. 若下调后仍不可靠，备选：在音频进入 YAMNet
  前做一次谱减法降噪（用一段纯白噪音作为噪声基线）

  > 这条必须实测，不能推算。写进阶段 5 的实地调参任务。

  ### 8.4 心跳设计（宪法第四条）

  **为什么是刚需**：家长不在家时，"程序崩溃了"和"一切正常没事发生"在手机上**长得一模一
  样**——都是没消息。

  心跳把"没消息"从两义变成一义：**收不到心跳 = 出事了。**

  每小时一条，内容含时间戳。家长只要养成"中午没看到心跳就查一下"的习惯，NFR-06（崩溃后
  1 小时内察觉）就达成了。

  ### 8.5 冷却机制

  报警后 10 分钟内不再重复报警。

  > **实现教训（值得写进简历讲）**：初版把"上次报警时刻"初始化为 `0.0`，冷却判断写成
  `当前时刻 - 0 < 600`。因为 `time.time()` 返回的是 17 亿这样的大数，这个条件**永远为假
  **，冷却逻辑实际从未生效——但功能表现"碰巧正确"，因为第一次报警时本来也不该冷却。
  >
  > **这类"靠巧合正确"的代码是典型技术债**：它今天能跑，但任何人改动时间基准就会炸。已
  改为初始化 `None` 并显式判空。

  ### 8.6 通知发送设计

  **🔴 级告警拆两条连发**（因为企业微信的 `image` 类型不支持 @所有人）：

  ```
  第 1 条（text，mentioned_list=["@all"]）：
    🔴 宝宝已经哭了 3 分钟，画面里没有人
    时间：2026-07-23 11:32:15
    建议打个电话回家看看情况。

  第 2 条（image）：
    [触发时刻的抓拍图]
  ```

  **发送保障**：
  - 失败重试 3 次，指数退避（1s / 4s / 16s）
  - 三次失败 → 写入待补发队列，网络恢复后补发，文案带「⚠️ 补发（原发生时间
  XX:XX）」前缀
  - **节流**：逼近 20 条 / 分钟上限时合并为一条汇总消息，不丢事件
  - **通知失败绝不导致主程序退出**（FR-07）

  ### 8.7 视觉感知设计（阶段 2–3）

  #### 8.7.1 成人与婴儿的区分

  一次检测输出画面中所有人的框与关键点。区分方法：

  ```
  成人判定 = 人体框高度 > 已标定床长的 1.2 倍
          或 检测框中心持续位于床区域之外且尺寸偏大
  ```

  **理由**：床的尺寸在配置时已标定，是画面中**唯一可靠的"尺子"**。婴儿躺在床里、成人站
  在床边，两者在画面中的尺度差异极大——用尺度判断比训练一个"成人 /
  婴儿"分类器**可靠得多，且零成本**。

  #### 8.7.2 俯卧与遮脸判定（R1 / R2 / R7）

  **核心思路：只看头，不看全身。** 盖被子后躯干关键点会大量丢失，但头一般露在外面。

  用头部 5 个关键点（鼻、左右眼、左右耳）的**可见性组合**判定朝向：

  | 可见关键点组合 | 判定 |
  |---|---|
  | 鼻 + 双眼 可见 | 仰卧（脸朝上）→ 正常 |
  | 鼻 + 单眼 + 单耳 | 侧卧 → 正常 |
  | 鼻、眼均不可见，双耳 / 头顶轮廓可见 | **俯卧（脸朝下）** → R1 计时 |
  | 头部区域整体检测不到，但躯干在床上 | **头被埋住** → R1 计时 |

  **遮脸（R2）**：以头部关键点外扩为 ROI，检测该 ROI 是否被"非人体物体"覆盖——与空床基线
  （§8.7.4）差分出现大面积新增区域，且不属于躯干连通区。

  > **夜视适配（关键）**：红外画面是灰度的，**不能用肤色做任何判断**。以上方案全部基于
  关键点几何和前景差分，不依赖颜色，**天生兼容夜视**。这是选择该方案而非颜色方案的决定
  性原因。

  **防抖**：关键点置信度 < 0.3 一律视为不可见；连续 3 帧（约 0.6
  秒）判定一致才更新状态。

  #### 8.7.3 越界判定（R4 / R5 / R8）

  一次性配置工具（FR-16）让用户在画面上框出两样东西：**床面区域**、**栏杆顶沿线**。

  | 规则 | 判定 |
  |---|---|
  | R4 婴儿离开画面 | 人体框与床区域交并比 < 0.2，持续 60 秒 |
  | R5 正在翻越 | 躯干中心（双肩 + 双髋中点）越过栏杆顶沿线，持续 3 秒 |
  | R8 卡栏杆 | 手腕 / 脚踝关键点位于床区域外，且 20 秒内位移 < 阈值（**不动 =
  卡住，在动 = 在玩**） |

  #### 8.7.4 空床基线与异物检测（R6，阶段 6）

  **基线学习**：在"床区域内检测不到人体"的时段采集帧，取最近 N 帧的**逐像素中值**作为背
  景模型（中值天然排除偶发遮挡）。每天在宝宝不在床上时自动更新一次，适应床品更换。

  **异物判定**：当前帧与背景模型差分 → 前景 mask → 减去人体包络 → 剩余连通域面积落在
  [下限, 上限] 之间（太小是噪点，太大是被子枕头）→ 持续 30 秒 → 🟡 告警。

  **分辨率决定可行性**（见 §10.2）：2K 画面覆盖 150cm 宽的床 ≈ **17.1 像素 /
  厘米**，一颗 1.5cm 的花生约 **26 像素**，刚压在可检测下限（约 30
  像素）上。**本功能只能检测 ≥ 1.5cm
  的物体，更小的看不到——这条必须写进用户文档，不能让用户以为它什么都能发现。**

  #### 8.7.5 画面异常检测（S-3）

  | 异常 | 判据 |
  |---|---|
  | 镜头被遮挡 | 全画面拉普拉斯方差骤降且持续 |
  | 全黑 | 平均亮度极低**且方差极小**（正常夜视有红外补光，画面有纹理，方差不会塌） |
  | 画面卡死 | 连续 N 帧帧间差分接近 0（真实静止画面也有传感器噪声，**完全为 0
  说明流卡了**） |
  | 断流 | 读取超时 |

  任一命中 → **立即停止全部视觉规则**，发一条
  🟡「摄像头看不清了」。**绝不在异常画面上继续做行为判断。**

  ### 8.8 大模型二次确认（阶段 4）

  本地层判定"疑似"后，抓 2–3 帧发给 Claude
  API，问一个封闭问题（例："这张图里的婴儿是脸朝下趴着吗？只回答
  是/否/看不清"），确认后才推送。

  - **上传前裁剪到婴儿床区域**，不传房间其他部分
  - **绝不传连续流**，只传触发时刻的 2–3 帧
  - API 失败 / 超时 → **降级为直接推送**（宁可多报一次，也不能因为 API
  挂了而漏掉真事件）
  - 成本估算：假设每天 20 次可疑触发 × 3 帧 ≈ 每月几元，满足宪法第十条

  ### 8.9 配置与密钥管理（宪法第七条）

  | 规则 | 说明 |
  |---|---|
  | Webhook key **等同于密码** | 拿到的人可以往你的群里发任何东西 |
  | **绝不提交进 Git** | 必须写进 `.gitignore` |
  | 代码里只保留占位符 | 提供 `config.example.py` 供他人参考 |
  | Claude API Key 同上 | 泄露会产生真实账单 |

  > 这是上传 GitHub 前的**硬性检查项**。密钥泄漏到公开仓库是最常见的新手事故，而且
  **Git 有历史记录——提交过再删掉是没用的，仍然能被翻出来。**

  ### 8.10 日志

  每秒一行
  CSV：时间戳、哭声分数、呜咽分数、开心分数、最像的类别、窗口比例、当前状态、已哭秒数。

  **用途**：调参依据、误报复盘、以及**"我不是拍脑袋定的阈值"的证据**（宪法第八条）。

  ---

  ## 9. 数据与隐私

  ### 9.1 原则与落地

  | 原则 | 落地 |
  |---|---|
  | 知情 | **必须告知看护人**摄像头存在及用途 |
  | 最小采集 | 镜头**只对准婴儿床**，不拍房间其他区域 |
  | 最小留存 | **仅保存触发时刻的画面**，不做连续录像，不存事件前后录像 |
  | 不出户 | 视频流不离开家庭网络 |
  | 密钥保护 | 见 §8.9 |

  ### 9.2 ⚠️ 哪些数据会离开你家（如实列清）

  "视频不出户"有**两条例外**，必须写明，不能含糊：

  | # | 什么数据 | 去哪 | 频率 | 为什么接受 |
  |---|---|---|---|---|
  | 例外 1 | 🔴 级告警的**一张抓拍图** | 腾讯服务器（企业微信） | 每次红色告警 1 张 |
  一眼看清现场，省掉一次往返。用户已知情并同意 |
  | 例外 2 | 可疑时刻的 **2–3 帧**（裁剪到床区域） | **Anthropic 服务器（境外）** |
  每次可疑触发 | 换取误报率大幅下降（宪法第二条）。用户已知情并同意 |

  **除以上两项外，任何图像、视频、音频都不离开家庭网络。**

  > **例外 2 需要特别注意**：这些帧会传输到**中国境外**。如果将来这个项目要送给别人用、
  或者公开发布，这一点必须在显著位置告知，让对方自己决定是否开启第二层。

  ### 9.3 本地留存

  | 数据 | 策略 |
  |---|---|
  | CSV 打分日志 | 长期保留（体积小，是调参的唯一依据） |
  | 触发时刻抓拍图 | 保留 30 天后自动清理 |
  | 连续录像 | **不产生**（宪法第六条） |

  ---

  ## 10. 部署与运行环境

  ### 10.1 运行环境

  | 项 | 内容 |
  |---|---|
  | **主机** | **家中已有的 Windows PC**（不购置新硬件，ADR-01） |
  | Python | 3.10+ |
  | 依赖 | `mediapipe`、`numpy`、`sounddevice`；阶段 2 起加 `opencv-python` |
  | 国内加速 | pip 使用清华镜像 `https://pypi.tuna.tsinghua.edu.cn/simple` |
  | 模型分发 | `yamnet.tflite` **随包分发**（托管在 Google Storage，国内无法直接下载）
  |
  | ⚠️ 注意 | 必须**关闭 Windows 电源计划的休眠**，否则电脑睡了程序就停了 |

  ### 10.2 摄像头要求

  | 指标 | 要求 | 当前状态 | 理由 |
  |---|---|---|---|
  | **协议** | 必须支持 **RTSP 或 ONVIF** | ✅ **已确认支持** | 硬性要求。只能用厂商
  App 的摄像头，程序读不到画面 |
  | **分辨率** | ≥ 2K | ✅ **2K** | **分辨率决定能否看清小物件**（§8.7.4）。2K 下 1.5cm
  物体 ≈ 26 像素，勉强够 |
  | **红外波长** | **940nm**，不要 850nm | ⬜ **待确认型号后核实** | 850nm
  会发出可见的暗红光，影响婴儿睡眠；940nm 完全不可见 |
  | 麦克风 | 有无均可 | — | 阶段 1 用的是 PC 麦克风，不依赖摄像头音频 |
  | 视角 | 覆盖整张婴儿床 + 床边一圈 | ⬜ 安装时确认 |
  视角决定覆盖范围，**分辨率决定小物件可检测性**，两者是不同的事 |
  | 安装 | 固定安装，位置不可随意移动 | ⬜ |
  摄像头一移动，床区域标定就失效，必须重新标定 |

  > **⬜ 待办**：摄像头具体型号未记录。需确认红外波长是 940nm 还是 850nm——如果是
  850nm，夜间会有可见红光，虽不影响功能但可能影响宝宝睡眠。

  ### 10.3 分发方式（NFR-08）

  Windows 批处理封装，非技术用户**双击即可，不需要敲命令行**：

  | 文件 | 作用 |
  |---|---|
  | `0-一键安装.bat` | 建虚拟环境、装依赖（走清华镜像） |
  | `1-查看麦克风.bat` | 列出所有麦克风及编号 |
  | `2-测试.bat` | 实时显示打分，用于调阈值，**不报警** |
  | `3-正式运行.bat` | 正式运行 |
  | `4-标定床区域.bat` | （阶段 3）弹出画面，鼠标拖框标定床与栏杆 |

  ---

  ## 11. 测试策略

  ### 11.1 已实现的测试（阶段 1，**6 / 6 通过**）

  | # | 测试 | 验证什么 |
  |---|---|---|
  | 1 | 短暂哭 20 秒 | 不该报警 → **防误报**（宪法第二条） |
  | 2 | 持续哭 5 分钟 | 该报 1 次 → 主功能 |
  | 3 | 哭中停顿换气 | 不该判成"停止哭" → **迟滞有效性** |
  | 4 | 连续哭 20 分钟 | 只报 2 次 → **冷却有效性** |
  | 5 | 白噪声 / 静音打分 | 分数 0.0000 → 不误判 |
  | 6 | webhook 未配置 | 返回 False 不崩溃 → 容错 |

  ### 11.2 尚未覆盖（技术债，需补）

  - 真实婴儿哭声录音的**召回率**测试
  - **白噪音机开启状态下**的哭声召回率（§8.3，最高优先级）
  - 长时间运行的内存泄漏 / 稳定性
  - 断网时的推送重试与补发
  - 摄像头断流恢复
  - 视觉规则的自动化测试（宪法第九条要求，阶段 2 起必须同步补）

  ### 11.3 实地调参（阶段 5）

  连续运行**一周**，用 CSV 日志复盘每一次误报与漏报，逐步调整 `CRY_THRESHOLD`，目标
  NFR-01（< 1 次 / 晚）。

  **必测项**：白噪音机开着时的真实哭声打分分布（§8.3）。

  ### 11.4 视觉部分的验收（阶段 3）

  | 项 | 标准 |
  |---|---|
  | R1 俯卧判定召回率 | ≥ 95%（可用婴儿仿真娃娃摆拍补充样本，避免刻意制造危险场景） |
  | R2 遮脸判定召回率 | ≥ 90% |
  | 成人检测 | 成人入画识别率 ≥ 95%，不把婴儿误判为成人 |
  | 夜视有效性 | **以上指标在红外黑白画面下同样成立**（主场景虽是白天，但夜间也会运行）
  |
  | 整机误报 | **连续 7 个真实日夜，人工复核每条告警，日均 ≤ 1 次** |

  ---

  ## 12. 风险与已知限制

  ### 12.1 三条必须说清楚的话

  1. **这是"省得你盯屏幕"的助手，不是安全设备。** 它不能预防婴儿猝死，不能替代看护人。
  2. **杀死这类系统的是误报，不是漏报。** 15 次告警里 14 次是假的，你第二天就会把它静音
  ——从此它一次也救不了你。所以**所有设计都向"宁可漏报也不误报"倾斜**。
  3. **绝不能做"没动静 = 没呼吸"的告警。** 睡着的婴儿本来就几乎不动。

  ### 12.2 误食：一个必须承认做不到的事

  **原始需求**：判断宝宝是否正在吃异物，及时报警。

  **为什么做不到**：完整告警链条 = 检测 → 推送 → 家长注意到手机 → 打电话 →
  看护人跑过去。这条链的耗时**必然超过窒息的 1–4 分钟黄金窗口**。

  > **做一个假的防御，比什么都不做更危险**——因为你会因此放松警惕。（宪法第五条）

  **改成什么**：从"急救"改成**预防性巡检**——婴儿床内出现白名单外的小物件时就告警（R6）
  。**这时候东西还没进嘴，推送延迟完全来得及。**

  **能力边界**：只能发现 ≥ 1.5cm
  的物体（§8.7.4）。**真正防误食的办法是床上别放小物件**，本功能只是补漏。

  ### 12.3 推送穿不透勿扰模式

  微信和企业微信**都无法**穿透 Android 勿扰 / iOS 专注模式或静音。

  - 能穿透的只有：Bark（iOS `level=critical`）、自建 ntfy（Android
  独立通知渠道）、或真人打电话
  - **当前接受这个限制**，因为主场景是白天不在家，手机不会静音
  - **代价**：夜间场景（§3.3）不做叫醒承诺
  - **预留升级路径**：通知层按多通道接口设计，将来接入 Bark / ntfy / 电话不改动其他模块

  ### 12.4 R8（卡栏杆）是误报风险最高的规则

  摄像头很难分清"手卡住了"和"就是把手搭在栏杆上不动"。

  **应对**：先按 20 秒 +
  位移阈值实现，上线后单独统计它的误报次数。若单这一条就吃掉了"每晚 < 1
  次"的预算，**按宪法第二条下线它**，而不是牺牲整体指标。

  ### 12.5 其他风险

  | 风险 | 应对 |
  |---|---|
  | 程序静默死亡 | 心跳（R11） |
  | Windows 休眠导致停止 | 关闭电源计划的休眠；文档中列为安装必做项 |
  | 断网 | 本地缓冲 + 恢复后补发（S-4） |
  | 摄像头被遮挡 / 移位 | 画面异常检测（S-3）+ R4 部分覆盖 |
  | **白噪音淹没哭声** | 实地调参 + CSV 复盘（§8.3）。**当前最主要的漏报风险** |
  | 通用姿态模型对婴儿效果下降 | 婴儿身体比例与成人差异大，且常被被子遮挡。阶段 3
  开工前必须先用真实录像验证头部关键点可用率 ≥ 85%，不通过则改用头部专用检测器 |
  | Claude API 境外访问不稳定 | 失败即降级为直接推送，不阻塞主链路 |

  ---

  ## 13. 路线图与任务拆解

  | 阶段 | 内容 | 状态 |
  |---|---|---|
  | **1** | 哭声检测 + 微信推送 + 心跳 + CSV 日志 | ✅ **已完成，6/6 测试通过** |
  | **2** | 摄像头接入（RTSP）+ 断流重连 + 画面异常检测 + 成人检测 → 规则 R3
  联合判定、R9、R10 | ⬜ 下一步 |
  | **3** | 婴儿姿态（俯卧 / 遮脸 / 出框）+ 越界（R5/R8）+ 床区域标定工具 + 抓拍 →
  R1/R2/R4/R5/R7/R8 | ⬜ |
  | **4** | Claude API 二次确认 | ⬜ |
  | **5** | **一周实地调参**，达成误报 < 1 次 / 晚（含白噪音场景实测） | ⬜ |
  | **6** | 空床基线 + 床内异物白名单扫描 → R6 | ⬜ |

  ### 13.1 阶段 2 的前置条件（开工前必须做完）

  | # | 事项 | 完成标准 |
  |---|---|---|
  | P-1 | 记录摄像头型号，核实红外波长 | 确认是 940nm 还是 850nm |
  | P-2 | 拿到 RTSP 地址并实测拉流 | 能连续拉流 30 分钟不断，且能读出实际分辨率与帧率 |
  | P-3 | 摄像头固定安装到位 | 覆盖整张床 + 床边一圈，安装后不再移动 |
  | P-4 | 告知看护人摄像头存在及用途 | 宪法第六条要求 |

  ### 13.2 阶段 3 的卡点验证

  用真实录像（白天 + 夜视各 30 段）验证头部关键点可用率 ≥ 85%。**不通过就不进入阶段 3
  开发**，改走头部专用检测器路线。带着不确定性往下写代码是这个项目最大的返工风险。

  ---

  ## 14. 设计决策记录（ADR）

  > **这一章是面试时要讲的内容。**
  每条记录都是"我面对什么矛盾、有哪些选项、我选了哪个、为什么"。
  > 所有决策日期均为 2026-07-23。

  | # | 决策 | 备选方案 | 为什么这么选 |
  |---|---|---|---|
  | **ADR-01** | **跑在家里现有的 Windows PC 上** | 买 N100 迷你主机（¥900–1400）/
  树莓派 5 | 家里已有 PC，算力足够。买新硬件既违反"不增加成本"，也让非技术用户多一道装
  机门槛（NFR-08）。**能不引入的复杂度就不引入。** |
  | **ADR-02** | **主场景定为白天（家长上班、看护人在家）** | 夜间为主 / 日夜同等 |
  用户实际痛点是"我不在，宝宝哭了有没有人管"。定为白天后，"微信穿不透静音"从致命缺陷降
  级为可接受限制（§12.3），整个通知方案得以成立 |
  | **ADR-03** | **哭声告警必须是联合规则：哭 3 分钟 + 全程无成人** | 哭 1 分钟就报 /
  哭就报 |
  白天有人带，宝宝哭一分钟完全正常。**单纯"哭"不是异常，"哭了没人管"才是异常。**
  这一条把误报率降了一个数量级，是宪法第二条的直接产物 |
  | **ADR-04** | **通知走企业微信群机器人** | 自建服务器 / 微信小程序 / Bark / ntfy |
  纯出站 HTTPS，不需要公网 IP、端口映射、域名、备案（NFR-05）；免费；iOS+Android
  双端；家人用普通微信扫码进群即可。**代价是穿不透勿扰模式，已接受** |
  | **ADR-05** | **两层级联：本地筛查 + Claude API 复核** | 全程调 API / 纯本地 |
  全程调 API 每月数千元（破宪法第十条）；纯本地误报多到没法用（破宪法第二条）。**级联是
  同时满足这两条的唯一结构。** 本地挡掉 99% 的平静时间，API 只在 1% 的可疑时刻被唤醒 |
  | **ADR-06** | **不做网页端 / 直播 / 手机 App** | 自研 WebRTC 直播 + 内网穿透（约 12
  天工作量） | 用户明确表示：收到报警看一眼照片就够，想看现场直接开摄像头自带的
  App。**摄像头厂商已经把这件事做好了，重做一遍只会多一个对外的口子（违反
  NFR-05）和多一份维护负担。** 省下 12 天工作量 |
  | **ADR-07** | **异物检测在"东西出现在床上"时就报，不等它送到嘴边** | 检测"手往嘴边送
  + 床上有异物" | 后者是"急救"，而急救链路必然赶不上窒息窗口（§12.2）——那是**假防御**
  ，违反宪法第五条。前者是"预防性巡检"，东西还没进嘴，推送延迟完全来得及 |
  | **ADR-08** | **只保存触发时刻的单帧，不存事件前后录像** | 存事发前 10 秒 + 后 10
  秒短视频 | 宪法第六条"仅保存触发时刻的画面"。短视频虽然便于复盘，但它是"连续录像"的滑
  坡起点，且抓拍图 + CSV 日志已足够复盘。**隐私红线不为便利让步** |
  | **ADR-09** | **AI 复核用境外 Claude API** | 国内大模型（通义/豆包/智谱）/ 不用 |
  用户明确选择 Claude。**代价是宝宝的照片会传输到境外**，已在 §9.2 例外 2
  显式记录。若将来分享给他人使用，必须在显著位置告知 |
  | **ADR-10** | **报警消息附带抓拍图** | 只发文字 | 一眼看清现场，省掉一次"打开 App
  确认"的往返。代价是图片经腾讯服务器（§9.2 例外 1），已接受。**保留一键关闭开关** |
  | **ADR-11** | **新增 R5（翻越）/ R8（卡栏杆）两条规则** | 沿用原设计"6
  个月不会爬，不做" | 6 个月翻越栏杆确实几乎不可能，但**手脚卡栏杆是真实风险**，且翻越
  能力几个月内就会出现——现在做等于提前投资。R8
  误报风险高，单独监控其误报次数，超标即下线（§12.4） |
  | **ADR-12** | **用"人体框高度 vs 床长"区分成人与婴儿**，不训分类器 |
  训练一个成人/婴儿分类器 | 床的尺寸已标定，是画面中**唯一可靠的尺子**。婴儿躺床里、成
  人站床边，尺度差异极大。**零训练成本、零数据需求、可解释**，比分类器可靠 |
  | **ADR-13** | **姿态判定只用头部关键点，不用全身** | 用全身 17 个关键点 |
  盖被子后躯干关键点大量丢失，而头一般露在外面。**且头部方案不依赖颜色，天生兼容红外灰
  度夜视**——这是决定性因素 |
  | **ADR-14** | **迟滞状态机（双阈值）判定哭 / 不哭** | 单一阈值 |
  宝宝哭会停下来换气，单阈值会让状态疯狂横跳、计时器不断重置，**永远攒不够 3
  分钟**。双阈值中间留缓冲带，换气落在带内不触发退出（§8.1） |
  | **ADR-15** | **模型全部选 Apache-2.0 / MIT / BSD，婴儿专用模型仅因自用才采纳** |
  直接用现成的开源婴儿监护项目 | 踩过一个坑：某项目仓库标 MIT，但代码 `import
  ultralytics`（AGPL-3.0）。**仓库的许可证不能给它引入的依赖重新授权。**
  本项目自用（NG7）故非商用模型可用，但**一旦商业化整条视觉链必须重做**——这一点必须写进
  README |

  ---

  ## 15. 附录

  ### 15.1 术语表（大白话）

  | 术语 | 大白话 |
  |---|---|
  | 迟滞（Hysteresis） | 开和关用两个不同的阈值，中间留一段缓冲，防止反复横跳 |
  | 滑动窗口 | 只看"最近 30 秒"，每过 1 秒扔掉最老的一条 |
  | 冷却（Cooldown） | 报过一次警之后，一段时间内不再重复报 |
  | 心跳（Heartbeat） | 系统定期说一句"我还活着" |
  | 级联（Cascade） | 便宜的先筛一遍，只有可疑的才交给贵的复查 |
  | 误报（False Positive） | 没事却报警了 |
  | 漏报（False Negative） | 有事却没报警 |
  | 召回率（Recall） | 100 次真事件里，系统抓住了几次 |
  | RTSP | 摄像头的通用视频流协议，有它别的程序才能读到画面 |
  | Webhook | 一个网址，往它发一条消息，消息就出现在你的微信群里 |
  | 关键点（Keypoint） | AI 在人身上标出的鼻子、眼睛、肩膀、手腕这些位置点 |
  | 前景差分 | 拿现在的画面减去"空床时的画面"，剩下的就是新出现的东西 |

  ### 15.2 开源资产清单

  | 资产 | 用途 | 许可证 | 本项目可用 |
  |---|---|---|---|
  | YAMNet | 音频分类 | Apache-2.0 | ✅ |
  | MediaPipe | 推理框架 / 姿态 | Apache-2.0 | ✅ |
  | PANNs | 音频分类备选 | MIT | ✅ |
  | AST | 音频分类备选 | BSD | ✅ |
  | RT-DETR / RF-DETR | 目标检测 | Apache-2.0 | ✅ |
  | mmpose | 姿态框架 | Apache-2.0 | ✅ |
  | ACLab Infant-Pose-Estimation | 婴儿姿态 | 非商用 | ✅（仅自用） |
  | ACLab CribNet | 婴儿床场景 | 非商用 | ✅（仅自用） |
  | SyRIP / MINI-RGBD | 婴儿数据集 | 非商用 | ✅（仅自用） |
  | AggPose | 婴儿姿态 | 非商用 | ✅（仅自用） |
  | ultralytics YOLO | 目标检测 | **AGPL-3.0** | ⚠️ 自用可，分发需谨慎 |

  ### 15.3 企业微信群机器人怎么弄（照着做即可）

  1. 手机应用商店下载 **企业微信**，用微信扫码登录，选「创建企业」，规模选最小的，名称
  随便填（比如"家庭"）。
  2. 在企业微信里**新建一个群聊**（至少 3 人）。
  3. 家人不用装企业微信——群设置里选「**邀请微信用户加入**」生成二维码，家人用**普通微信
  **扫码就能进群收消息。
  4. 进群 →「**⋯**」→「**群机器人**」→「**添加机器人**」→「**新创建一个机器人**」→
  起名（如"宝宝监护"）。
  5. 复制生成的 **Webhook 地址**（形如
  `https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxx`），填进程序配置。
  6. ⚠️ **这个地址等于钥匙，不要发到任何公开地方，不要提交进 Git。**
  7. 群聊设置里**关闭「消息免打扰」**、**打开「置顶聊天」**，并设置一个响亮的独立提示音
  ；手机系统设置里允许企业微信通知。

  ### 15.4 参考资料

  - YAMNet /
  AudioSet：https://github.com/tensorflow/models/tree/master/research/audioset/yamnet
  - MediaPipe Audio
  Classifier：https://ai.google.dev/edge/mediapipe/solutions/audio/audio_classifier
  - 企业微信群机器人：https://developer.work.weixin.qq.com/document/path/91770
  - ACLab 婴儿姿态：https://github.com/ostadabbas

  ---

  ## 变更记录

  | 版本 | 日期 | 变更 |
  |---|---|---|
  | v1.0 | 2026-07-23 | `DESIGN.md` 初版，覆盖阶段 1 实现与阶段 2–6 设计 |
  | v2.1 | 2026-07-30 | 新增 §7.4 视频接入与开发环境（RTSP+OpenCV+Python
  3.14+venv，均已实测）；原 §7.4 被否决方案顺延为 §7.5。同日新增派生文档
  `tech.md`（GitHub 门面版技术清单，以本文为准） |
  | **v2.0** | **2026-07-23** | **合并六份文档为单一 SDD。** 并入项目宪法（§2）、001
  哭声规格、002 摄像头规格与方案。消解 14 处文档间冲突，全部记入 §14
  ADR。新增：白噪音漏报风险分析（§8.3）、数据出境清单（§9.2）、越界规则
  R5/R8、设计决策记录章节 |

  ---

  ## 待办清单（本文档中所有未定项）

  | # | 事项 | 归属 |
  |---|---|---|
  | 1 | 记录摄像头型号，核实红外波长（940nm / 850nm） | §10.2 / P-1 |
  | 2 | 实测 RTSP 拉流，确认实际分辨率与帧率 | P-2 |
  | 3 | 实测白噪音机开启时的真实哭声打分分布，据此下调 `CRY_THRESHOLD` | §8.3 |
  | 4 | 补齐阶段 1 缺失的测试（召回率、内存泄漏、断网补发） | §11.2 |
  | 5 | 补齐视觉规则的自动化测试（宪法第九条） | §11.2 |
  | 6 | 阶段 3 开工前验证头部关键点可用率 ≥ 85% | §13.2 |
  | 7 | 告知看护人摄像头存在及用途 | P-4 |
  | 8 | 上传 GitHub 前逐项检查密钥零提交 | §8.9 |